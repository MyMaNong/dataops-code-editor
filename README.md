# Rslib project

## Setup

Install the dependencies:

```bash
yarn install
```

## Get started

Build the library:

```bash
yarn run build
```

Build the library in watch mode:

```bash
yarn run dev
```
