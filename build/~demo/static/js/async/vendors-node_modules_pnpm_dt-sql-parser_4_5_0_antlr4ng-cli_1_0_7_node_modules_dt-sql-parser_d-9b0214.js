"use strict";
(self["rspackChunk_dataops_code_editor"] = self["rspackChunk_dataops_code_editor"] || []).push([["vendors-node_modules_pnpm_dt-sql-parser_4_5_0_antlr4ng-cli_1_0_7_node_modules_dt-sql-parser_d-9b0214"], {
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/lib/SQLParserBase.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  SQLParserBase: () => (SQLParserBase)\n});\n/* import */ var antlr4ng__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/antlr4ng@2.0.11_antlr4ng-cli@1.0.7/node_modules/antlr4ng/dist/index.mjs\");\n\nclass SQLParserBase extends antlr4ng__rspack_import_0.Parser {\n    constructor(input) {\n        super(input);\n        this.caretTokenIndex = -1;\n        this.entityCollecting = false;\n    }\n    /**\n     * Semantic predicate to determine whether to match empty column.\n     *\n     * Key design:\n     * 1. Only match empty column in entityCollecting mode\n     * 2. Check if caret position is at the empty column position\n     * 3. In validate mode (entityCollecting=false), this predicate returns false\n     *    and reports an error to ensure incomplete SQL is caught\n     *\n     * IMPORTANT: This predicate should be used carefully to avoid affecting\n     * prediction in non-entity-collecting contexts.\n     */\n    shouldMatchEmpty(ruleName) {\n        var _a, _b;\n        // Only match in entityCollecting mode or when caret position is specified (suggestion mode)\n        if (this.entityCollecting || this.caretTokenIndex >= 0) {\n            // If no caret position specified, match all empty columns\n            if (this.caretTokenIndex < 0) {\n                return true;\n            }\n            // Check if caret is at the position where empty column would be\n            const prevTokenIndex = (_a = this.tokenStream.LT(-1)) === null || _a === void 0 ? void 0 : _a.tokenIndex;\n            const nextTokenIndex = (_b = this.tokenStream.LT(1)) === null || _b === void 0 ? void 0 : _b.tokenIndex;\n            // Match if caret is between previous and next token\n            if (prevTokenIndex !== undefined && nextTokenIndex !== undefined) {\n                return prevTokenIndex <= this.caretTokenIndex && nextTokenIndex >= this.caretTokenIndex;\n            }\n            // If only previous token exists, match if caret is after it\n            if (prevTokenIndex !== undefined) {\n                return prevTokenIndex <= this.caretTokenIndex;\n            }\n            // If only next token exists, match if caret is before it\n            if (nextTokenIndex !== undefined) {\n                return nextTokenIndex >= this.caretTokenIndex;\n            }\n            return false;\n        }\n        // In pure validate mode, don't match empty columns\n        // This allows ANTLR to report errors naturally\n        return false;\n    }\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/lib/SQLParserBase.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/locale/locale.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  i18n: () => (i18n)\n});\nconst zh_CN = {\n    stmtInComplete: '语句不完整',\n    noValidPosition: '在此位置无效',\n    expecting: '，期望',\n    unfinishedMultilineComment: '未完成的多行注释',\n    unfinishedDoubleQuoted: '未完成的双引号字符串字变量',\n    unfinishedSingleQuoted: '未完成的单引号字符串字变量',\n    unfinishedTickQuoted: '未完成的反引号引用字符串字变量',\n    noValidInput: '没有有效的输入',\n    newObj: '一个新的对象',\n    existingObj: '一个存在的对象',\n    new: '一个新的',\n    existing: '一个存在的',\n    orKeyword: '或者一个关键字',\n    keyword: '一个关键字',\n    missing: '缺少',\n    at: '在',\n    or: '或者',\n};\nconst en_US = {\n    stmtInComplete: 'Statement is incomplete',\n    noValidPosition: 'is not valid at this position',\n    expecting: ', expecting ',\n    unfinishedMultilineComment: 'Unfinished multiline comment',\n    unfinishedDoubleQuoted: 'Unfinished double quoted string literal',\n    unfinishedSingleQuoted: 'Unfinished single quoted string literal',\n    unfinishedTickQuoted: 'Unfinished back tick quoted string literal',\n    noValidInput: 'is no valid input at all',\n    newObj: 'a new object',\n    existingObj: 'an existing object',\n    new: 'a new ',\n    existing: 'an existing ',\n    orKeyword: ' or a keyword',\n    keyword: 'a keyword',\n    missing: 'missing ',\n    at: ' at ',\n    or: ' or ',\n};\nconst i18n = {\n    zh_CN,\n    en_US,\n};\n\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/locale/locale.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/basicSQL.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  BasicSQL: () => (BasicSQL),\n  SQL_SPLIT_SYMBOL_TEXT: () => (SQL_SPLIT_SYMBOL_TEXT)\n});\n/* import */ var antlr4_c3__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/antlr4-c3@3.3.7_antlr4ng-cli@1.0.7/node_modules/antlr4-c3/lib/index.mjs\");\n/* import */ var antlr4ng__rspack_import_1 = __webpack_require__(\"./node_modules/.pnpm/antlr4ng@2.0.11_antlr4ng-cli@1.0.7/node_modules/antlr4ng/dist/index.mjs\");\n/* import */ var _errorStrategy__rspack_import_2 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/errorStrategy.js\");\n/* import */ var _findCaretTokenIndex__rspack_import_4 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/findCaretTokenIndex.js\");\n/* import */ var _textAndWord__rspack_import_3 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/textAndWord.js\");\n\n\n\n\n\nconst SQL_SPLIT_SYMBOL_TEXT = ';';\n/**\n * Basic SQL class, every sql needs extends it.\n */\nclass BasicSQL {\n    constructor() {\n        this._parseErrors = [];\n        /** members for cache end */\n        this._errorListener = (error) => {\n            this._parseErrors.push(error);\n        };\n        this.locale = 'en_US';\n    }\n    /**\n     * Create an antlr4 lexer from input.\n     * @param input string\n     */\n    createLexer(input, errorListener) {\n        const charStreams = antlr4ng__rspack_import_1.CharStreams.fromString(input);\n        const lexer = this.createLexerFromCharStream(charStreams);\n        if (errorListener) {\n            lexer.removeErrorListeners();\n            lexer.addErrorListener(this.createErrorListener(errorListener));\n        }\n        return lexer;\n    }\n    /**\n     * Create an antlr4 parser from input.\n     * @param input string\n     */\n    createParser(input, errorListener) {\n        const lexer = this.createLexer(input, errorListener);\n        const tokenStream = new antlr4ng__rspack_import_1.CommonTokenStream(lexer);\n        tokenStream.fill();\n        const parser = this.createParserFromTokenStream(tokenStream);\n        parser.interpreter.predictionMode = antlr4ng__rspack_import_1.PredictionMode.SLL;\n        if (errorListener) {\n            parser.removeErrorListeners();\n            parser.addErrorListener(this.createErrorListener(errorListener));\n        }\n        return parser;\n    }\n    /**\n     * Parse input string and return parseTree.\n     * @param input string\n     * @param errorListener listen parse errors and lexer errors.\n     * @returns parseTree\n     */\n    parse(input, errorListener) {\n        const parser = this.createParser(input, errorListener);\n        parser.buildParseTrees = true;\n        parser.errorHandler = new _errorStrategy__rspack_import_2.ErrorStrategy();\n        return parser.program();\n    }\n    /**\n     * Create an antlr4 parser from input.\n     * And the instances will be cache.\n     * @param input string\n     */\n    createParserWithCache(input) {\n        this._parseTree = null;\n        this._charStreams = antlr4ng__rspack_import_1.CharStreams.fromString(input);\n        this._lexer = this.createLexerFromCharStream(this._charStreams);\n        this._lexer.removeErrorListeners();\n        this._lexer.addErrorListener(this.createErrorListener(this._errorListener));\n        this._tokenStream = new antlr4ng__rspack_import_1.CommonTokenStream(this._lexer);\n        /**\n         * All tokens are generated in advance.\n         * This can cause performance degradation, but it seems necessary for now.\n         * Because the tokens will be used multiple times.\n         */\n        this._tokenStream.fill();\n        this._parser = this.createParserFromTokenStream(this._tokenStream);\n        this._parser.interpreter.predictionMode = antlr4ng__rspack_import_1.PredictionMode.SLL;\n        this._parser.buildParseTrees = true;\n        this._parser.errorHandler = new _errorStrategy__rspack_import_2.ErrorStrategy();\n        return this._parser;\n    }\n    /**\n     * If it is invoked multiple times in a row and the input parameters is the same,\n     * this method returns the parsing result directly for the first time\n     * unless the errorListener parameter is passed.\n     * @param input source string\n     * @param errorListener listen errors\n     * @returns parseTree\n     */\n    parseWithCache(input, errorListener) {\n        // Avoid parsing the same input repeatedly.\n        if (this._parsedInput === input && !errorListener && this._parseTree) {\n            return this._parseTree;\n        }\n        this._parseErrors = [];\n        const parser = this.createParserWithCache(input);\n        this._parsedInput = input;\n        parser.removeErrorListeners();\n        parser.addErrorListener(this.createErrorListener(this._errorListener));\n        this._parseTree = parser.program();\n        return this._parseTree;\n    }\n    /**\n     * Validate input string and return syntax errors if exists.\n     * When input contains multiple statements separated by semicolons\n     * and the initial parse doesn't capture all errors, each statement\n     * is validated independently to collect all errors.\n     * @param input source string\n     * @returns syntax errors\n     */\n    validate(input) {\n        this.parseWithCache(input);\n        if (this._parseErrors.length > 0) {\n            const statements = this.splitStatements(input);\n            if (statements.length > 1) {\n                const splitErrors = this.validateStatements(statements);\n                if (splitErrors.length > this._parseErrors.length) {\n                    this._parseErrors = splitErrors;\n                }\n            }\n        }\n        return this._parseErrors;\n    }\n    /**\n     * Validate each statement fragment independently and collect all errors.\n     */\n    validateStatements(statements) {\n        const allErrors = [];\n        let lineOffset = 0;\n        for (const statement of statements) {\n            if (!statement.text.trim()) {\n                const newlines = (statement.text.match(/\\n/g) || []).length;\n                lineOffset += newlines;\n                continue;\n            }\n            const parser = this.createParser(statement.text);\n            const errors = [];\n            parser.removeErrorListeners();\n            parser.addErrorListener(this.createErrorListener((error) => {\n                errors.push({\n                    startLine: error.startLine + lineOffset,\n                    endLine: error.endLine + lineOffset,\n                    startColumn: error.startColumn,\n                    endColumn: error.endColumn,\n                    message: error.message,\n                });\n            }));\n            parser.errorHandler = new _errorStrategy__rspack_import_2.ErrorStrategy();\n            parser.program();\n            allErrors.push(...errors);\n            const newlines = (statement.text.match(/\\n/g) || []).length;\n            lineOffset += newlines;\n        }\n        return allErrors;\n    }\n    /**\n     * Split input into individual statement strings by semicolons.\n     * Handles semicolons inside quoted strings correctly.\n     */\n    splitStatements(input) {\n        const statements = [];\n        let inSingleQuote = false;\n        let inDoubleQuote = false;\n        let lastSplit = 0;\n        for (let i = 0; i < input.length; i++) {\n            const ch = input[i];\n            if (ch === \"'\" && !inDoubleQuote) {\n                inSingleQuote = !inSingleQuote;\n            }\n            else if (ch === '\"' && !inSingleQuote) {\n                inDoubleQuote = !inDoubleQuote;\n            }\n            else if (ch === ';' && !inSingleQuote && !inDoubleQuote) {\n                statements.push({ text: input.slice(lastSplit, i + 1), start: lastSplit });\n                lastSplit = i + 1;\n            }\n        }\n        if (lastSplit < input.length) {\n            statements.push({ text: input.slice(lastSplit), start: lastSplit });\n        }\n        return statements;\n    }\n    /**\n     * Get the input string that has been parsed.\n     */\n    getParsedInput() {\n        return this._parsedInput;\n    }\n    /**\n     * Get all Tokens of input string，'<EOF>' is not included.\n     * @param input source string\n     * @returns Token[]\n     */\n    getAllTokens(input) {\n        this.parseWithCache(input);\n        let allTokens = this._tokenStream.getTokens();\n        if (allTokens[allTokens.length - 1].text === '<EOF>') {\n            allTokens = allTokens.slice(0, -1);\n        }\n        return allTokens;\n    }\n    /**\n     * @param listener Listener instance extends ParserListener\n     * @param parseTree parser Tree\n     */\n    listen(listener, parseTree) {\n        antlr4ng__rspack_import_1.ParseTreeWalker.DEFAULT.walk(listener, parseTree);\n    }\n    /**\n     * Split input into statements.\n     * If exist syntax error it will return null.\n     * @param input source string\n     */\n    splitSQLByStatement(input) {\n        const errors = this.validate(input);\n        if (errors.length || !this._parseTree) {\n            return null;\n        }\n        const splitListener = this.splitListener;\n        this.listen(splitListener, this._parseTree);\n        const res = splitListener.statementsContext\n            .map((context) => {\n            return (0,_textAndWord__rspack_import_3.ctxToText)(context, this._parsedInput);\n        })\n            .filter(Boolean);\n        return res;\n    }\n    /**\n     * Get the smaller range of input\n     * @param input string\n     * @param allTokens all tokens from input\n     * @param tokenIndexOffset offset of the tokenIndex in the range of input\n     * @param caretTokenIndex tokenIndex of caretPosition\n     * @returns inputSlice: string, caretTokenIndex: number\n     */\n    splitInputBySymbolText(input, allTokens, tokenIndexOffset, caretTokenIndex) {\n        const tokens = allTokens.slice(tokenIndexOffset);\n        /**\n         * Set startToken\n         */\n        let startToken = null;\n        for (let tokenIndex = caretTokenIndex - tokenIndexOffset; tokenIndex >= 0; tokenIndex--) {\n            const token = tokens[tokenIndex];\n            if ((token === null || token === void 0 ? void 0 : token.text) === SQL_SPLIT_SYMBOL_TEXT) {\n                startToken = tokens[tokenIndex + 1];\n                break;\n            }\n        }\n        if (startToken === null) {\n            startToken = tokens[0];\n        }\n        /**\n         * Set stopToken\n         */\n        let stopToken = null;\n        for (let tokenIndex = caretTokenIndex - tokenIndexOffset; tokenIndex < tokens.length; tokenIndex++) {\n            const token = tokens[tokenIndex];\n            if ((token === null || token === void 0 ? void 0 : token.text) === SQL_SPLIT_SYMBOL_TEXT) {\n                stopToken = token;\n                break;\n            }\n        }\n        if (stopToken === null) {\n            stopToken = tokens[tokens.length - 1];\n        }\n        const indexOffset = tokens[0].start;\n        let startIndex = startToken.start - indexOffset;\n        let stopIndex = stopToken.stop + 1 - indexOffset;\n        /**\n         * Save offset of the tokenIndex in the range of input\n         * compared to the tokenIndex in the whole input\n         */\n        const _tokenIndexOffset = startToken.tokenIndex;\n        const _caretTokenIndex = caretTokenIndex - _tokenIndexOffset;\n        /**\n         * Get the smaller range of _input\n         */\n        const _input = input.slice(startIndex, stopIndex);\n        return {\n            inputSlice: _input,\n            allTokens: allTokens.slice(_tokenIndexOffset),\n            caretTokenIndex: _caretTokenIndex,\n        };\n    }\n    /**\n     * Get the minimum input string that can be parsed successfully by c3.\n     * @param input source string\n     * @param caretTokenIndex tokenIndex of caretPosition\n     * @param originParseTree origin parseTree\n     * @returns MinimumInputInfo\n     */\n    getMinimumInputInfo(input, caretTokenIndex, originParseTree) {\n        var _a, _b, _c, _d, _e, _f, _g, _h, _j;\n        if (!originParseTree || !(input === null || input === void 0 ? void 0 : input.length))\n            return null;\n        let inputSlice = input;\n        /**\n         * Split sql by statement.\n         * Try to collect candidates in as small a range as possible.\n         */\n        const splitListener = this.splitListener;\n        this.listen(splitListener, originParseTree);\n        const statementCount = (_a = splitListener.statementsContext) === null || _a === void 0 ? void 0 : _a.length;\n        const statementsContext = splitListener.statementsContext;\n        let tokenIndexOffset = 0;\n        // If there are multiple statements.\n        if (statementCount > 1) {\n            /**\n             * Find a minimum valid range, reparse the fragment, and provide a new parse tree to C3.\n             * The boundaries of this range must be statements with no syntax errors.\n             * This can ensure the stable performance of the C3.\n             */\n            let startStatement = null;\n            let stopStatement = null;\n            for (let index = 0; index < statementCount; index++) {\n                const ctx = statementsContext[index];\n                const isCurrentCtxValid = !ctx.exception;\n                if (!isCurrentCtxValid)\n                    continue;\n                /**\n                 * Ensure that the statementContext before the left boundary\n                 * and the last statementContext on the right boundary are qualified SQL statements.\n                 */\n                const isPrevCtxValid = index === 0 || !((_b = statementsContext[index - 1]) === null || _b === void 0 ? void 0 : _b.exception);\n                const isNextCtxValid = index === statementCount - 1 || !((_c = statementsContext[index + 1]) === null || _c === void 0 ? void 0 : _c.exception);\n                if (ctx.stop && ctx.stop.tokenIndex < caretTokenIndex && isPrevCtxValid) {\n                    startStatement = ctx;\n                }\n                if (ctx.start &&\n                    !stopStatement &&\n                    ctx.start.tokenIndex > caretTokenIndex &&\n                    isNextCtxValid) {\n                    stopStatement = ctx;\n                    break;\n                }\n            }\n            // A boundary consisting of the index of the input.\n            const startIndex = (_e = (_d = startStatement === null || startStatement === void 0 ? void 0 : startStatement.start) === null || _d === void 0 ? void 0 : _d.start) !== null && _e !== void 0 ? _e : 0;\n            const stopIndex = (_g = (_f = stopStatement === null || stopStatement === void 0 ? void 0 : stopStatement.stop) === null || _f === void 0 ? void 0 : _f.stop) !== null && _g !== void 0 ? _g : inputSlice.length;\n            /**\n             * Save offset of the tokenIndex in the range of input\n             * compared to the tokenIndex in the whole input\n             */\n            tokenIndexOffset = (_j = (_h = startStatement === null || startStatement === void 0 ? void 0 : startStatement.start) === null || _h === void 0 ? void 0 : _h.tokenIndex) !== null && _j !== void 0 ? _j : 0;\n            inputSlice = inputSlice.slice(startIndex, stopIndex);\n        }\n        return {\n            input: inputSlice,\n            tokenIndexOffset,\n            statementCount,\n        };\n    }\n    /**\n     * Get a minimum boundary parser near caretTokenIndex.\n     * @param input source string.\n     * @param caretTokenIndex start from which index to minimize the boundary.\n     * @param originParseTree the parse tree need to be minimized, default value is the result of parsing `input`.\n     * @returns minimum parser info\n     */\n    getMinimumParserInfo(input, caretTokenIndex, originParseTree) {\n        if (!originParseTree || !(input === null || input === void 0 ? void 0 : input.length))\n            return null;\n        const inputInfo = this.getMinimumInputInfo(input, caretTokenIndex, originParseTree);\n        if (!inputInfo)\n            return null;\n        const { input: inputSlice, tokenIndexOffset } = inputInfo;\n        caretTokenIndex = caretTokenIndex - tokenIndexOffset;\n        let sqlParserIns = this._parser;\n        let parseTree = originParseTree;\n        /**\n         * Reparse the input fragment,\n         * and c3 will collect candidates in the newly generated parseTree when input changed.\n         */\n        if (inputSlice !== input) {\n            sqlParserIns = this.createParser(inputSlice);\n            parseTree = sqlParserIns.program();\n        }\n        return {\n            parser: sqlParserIns,\n            parseTree,\n            tokenIndexOffset,\n            newTokenIndex: caretTokenIndex,\n        };\n    }\n    /**\n     * Get suggestions of syntax and token at caretPosition\n     * @param input source string\n     * @param caretPosition caret position, such as cursor position\n     * @returns suggestion\n     */\n    getSuggestionAtCaretPosition(input, caretPosition) {\n        this.parseWithCache(input);\n        if (!this._parseTree)\n            return null;\n        let allTokens = this.getAllTokens(input);\n        let caretTokenIndex = (0,_findCaretTokenIndex__rspack_import_4.findCaretTokenIndex)(caretPosition, allTokens);\n        if (!caretTokenIndex && caretTokenIndex !== 0)\n            return null;\n        const inputInfo = this.getMinimumInputInfo(input, caretTokenIndex, this._parseTree);\n        if (!inputInfo)\n            return null;\n        const { input: _input, tokenIndexOffset, statementCount } = inputInfo;\n        let inputSlice = _input;\n        /**\n         * Split the inputSlice by separator to get the smaller range of inputSlice.\n         */\n        if (inputSlice.includes(SQL_SPLIT_SYMBOL_TEXT)) {\n            const { inputSlice: _inputSlice, allTokens: _allTokens, caretTokenIndex: _caretTokenIndex, } = this.splitInputBySymbolText(inputSlice, allTokens, tokenIndexOffset, caretTokenIndex);\n            allTokens = _allTokens;\n            caretTokenIndex = _caretTokenIndex;\n            inputSlice = _inputSlice;\n        }\n        else {\n            if (statementCount > 1) {\n                caretTokenIndex = caretTokenIndex - tokenIndexOffset;\n                allTokens = allTokens.slice(tokenIndexOffset);\n            }\n        }\n        let sqlParserIns = this._parser;\n        let parseTree = this._parseTree;\n        /**\n         * Reparse the input fragment,\n         * and c3 will collect candidates in the newly generated parseTree when input changed.\n         */\n        if (inputSlice !== input) {\n            sqlParserIns = this.createParser(inputSlice);\n            parseTree = sqlParserIns.program();\n        }\n        const core = new antlr4_c3__rspack_import_0.CodeCompletionCore(sqlParserIns);\n        core.preferredRules = this.preferredRules;\n        // core.showRuleStack = true;\n        // core.showResult = true;\n        const candidates = core.collectCandidates(caretTokenIndex, parseTree);\n        const originalSuggestions = this.processCandidates(candidates, allTokens, caretTokenIndex);\n        const syntaxSuggestions = originalSuggestions.syntax.map((syntaxCtx) => {\n            const wordRanges = syntaxCtx.wordRanges.map((token) => {\n                return (0,_textAndWord__rspack_import_3.tokenToWord)(token, this._parsedInput);\n            });\n            return {\n                syntaxContextType: syntaxCtx.syntaxContextType,\n                wordRanges,\n            };\n        });\n        return {\n            syntax: syntaxSuggestions,\n            keywords: originalSuggestions.keywords,\n        };\n    }\n    getAllEntities(input, caretPosition) {\n        /**\n         * Create a new parser to generate brand new parse tree.\n         * And the new parse tree should not effect cached parse tree which is used by validate and getSuggestionAtCaretPosition method.\n         */\n        const parser = this.createParser(input);\n        const allTokens = parser.tokenStream.getTokens();\n        const caretTokenIndex = caretPosition\n            ? (0,_findCaretTokenIndex__rspack_import_4.findCaretTokenIndex)(caretPosition, allTokens)\n            : void 0;\n        const collectListener = this.createEntityCollector(input, allTokens, caretTokenIndex);\n        parser.entityCollecting = true;\n        if (caretPosition && caretTokenIndex !== undefined) {\n            parser.caretTokenIndex = caretTokenIndex;\n        }\n        const parseTree = parser.program();\n        this.listen(collectListener, parseTree);\n        parser.caretTokenIndex = -1;\n        parser.entityCollecting = false;\n        return collectListener.getEntities();\n    }\n    /**\n     * Get semantic context infos\n     * @param input source string\n     * @param caretPosition caret position, such as cursor position\n     * @param options semantic context options\n     * @returns analyzed semantic context\n     */\n    getSemanticContextAtCaretPosition(input, caretPosition, options) {\n        const allTokens = this.getAllTokens(input);\n        const parseTree = this.parseWithCache(input);\n        const statementContextListener = this.createSemanticContextCollector(input, caretPosition, allTokens, options);\n        this.listen(statementContextListener, parseTree);\n        return statementContextListener.semanticContext;\n    }\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/basicSQL.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/errorStrategy.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  ErrorStrategy: () => (ErrorStrategy)\n});\n/* import */ var antlr4ng__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/antlr4ng@2.0.11_antlr4ng-cli@1.0.7/node_modules/antlr4ng/dist/index.mjs\");\n\n/**\n * Base on DefaultErrorStrategy.\n * The difference is that it assigns exception to the context.exception when it encounters error.\n */\nclass ErrorStrategy extends antlr4ng__rspack_import_0.DefaultErrorStrategy {\n    recover(recognizer, e) {\n        // Mark the context as an anomaly\n        for (let context = recognizer.context; context; context = context.parent) {\n            context.exception = e;\n        }\n        // Error recovery\n        if (this.lastErrorIndex === recognizer.inputStream.index &&\n            this.lastErrorStates &&\n            this.lastErrorStates.contains(recognizer.state)) {\n            recognizer.consume();\n        }\n        this.lastErrorIndex = recognizer.inputStream.index;\n        if (!this.lastErrorStates) {\n            this.lastErrorStates = new antlr4ng__rspack_import_0.IntervalSet();\n        }\n        this.lastErrorStates.addOne(recognizer.state);\n        let followSet = this.getErrorRecoverySet(recognizer);\n        this.consumeUntil(recognizer, followSet);\n    }\n    recoverInline(recognizer) {\n        let e;\n        if (this.nextTokensContext === undefined) {\n            e = new antlr4ng__rspack_import_0.InputMismatchException(recognizer);\n        }\n        else {\n            e = new antlr4ng__rspack_import_0.InputMismatchException(recognizer);\n        }\n        // Mark the context as an anomaly\n        for (let context = recognizer.context; context; context = context.parent) {\n            context.exception = e;\n        }\n        // Error recovery\n        let matchedSymbol = this.singleTokenDeletion(recognizer);\n        if (matchedSymbol) {\n            recognizer.consume();\n            return matchedSymbol;\n        }\n        if (this.singleTokenInsertion(recognizer)) {\n            return this.getMissingSymbol(recognizer);\n        }\n        throw e;\n    }\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/errorStrategy.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/findCaretTokenIndex.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  findCaretTokenIndex: () => (findCaretTokenIndex)\n});\n/**\n * find token index via caret position (cursor position)\n * @param caretPosition\n * @param allTokens all the tokens\n * @returns caretTokenIndex\n */\nfunction findCaretTokenIndex(caretPosition, allTokens) {\n    var _a, _b;\n    const { lineNumber: caretLine, column: caretCol } = caretPosition;\n    let left = 0;\n    let right = allTokens.length - 1;\n    while (left <= right) {\n        const mid = left + ((right - left) >> 1);\n        const token = allTokens[mid];\n        if (token.line > caretLine || (token.line === caretLine && token.column + 1 >= caretCol)) {\n            right = mid - 1;\n        }\n        else if (token.line < caretLine ||\n            (token.line === caretLine && token.column + ((_b = (_a = token.text) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0) + 1 < caretCol)) {\n            left = mid + 1;\n        }\n        else {\n            return allTokens[mid].tokenIndex;\n        }\n    }\n    return void 0;\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/findCaretTokenIndex.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/parseErrorListener.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  ParseErrorListener: () => (ParseErrorListener)\n});\n/* import */ var antlr4ng__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/antlr4ng@2.0.11_antlr4ng-cli@1.0.7/node_modules/antlr4ng/dist/index.mjs\");\n/* import */ var _transform__rspack_import_1 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/transform.js\");\n\n\nclass ParseErrorListener {\n    get locale() {\n        return this.parserContext.locale;\n    }\n    constructor(errorListener, parserContext, preferredRules) {\n        this.parserContext = parserContext;\n        this.preferredRules = preferredRules;\n        this._errorListener = errorListener;\n    }\n    reportAmbiguity() { }\n    reportAttemptingFullContext() { }\n    reportContextSensitivity() { }\n    syntaxError(recognizer, offendingSymbol, line, charPositionInLine, msg, e) {\n        var _a;\n        let message = '';\n        // If not undefined then offendingSymbol is of type Token.\n        if (offendingSymbol) {\n            let token = offendingSymbol;\n            const parser = recognizer;\n            // judge token is EOF\n            const isEof = token.type === antlr4ng__rspack_import_0.Token.EOF;\n            if (isEof) {\n                token = parser.tokenStream.get(token.tokenIndex - 1);\n            }\n            const wrongText = (_a = token.text) !== null && _a !== void 0 ? _a : '';\n            const isInComplete = isEof && wrongText !== ' ';\n            const expectedText = isInComplete ? '' : this.getExpectedText(parser, token);\n            if (!e) {\n                // handle missing or unwanted tokens.\n                message = msg;\n                if (msg.includes('extraneous')) {\n                    message = `'${wrongText}' {noValidPosition}${expectedText.length ? `{expecting}${expectedText}` : ''}`;\n                }\n                if (msg.includes('missing')) {\n                    const regex = /missing\\s+'([^']+)'/;\n                    const match = msg.match(regex);\n                    message = `{missing}`;\n                    if (match) {\n                        const missKeyword = match[1];\n                        message += `'${missKeyword}'`;\n                    }\n                    else {\n                        message += `{keyword}`;\n                    }\n                    message += `{at}'${wrongText}'`;\n                }\n            }\n            else {\n                // handle mismatch exception or no viable alt exception\n                if (e instanceof antlr4ng__rspack_import_0.InputMismatchException || e instanceof antlr4ng__rspack_import_0.NoViableAltException) {\n                    if (isEof) {\n                        message = `{stmtInComplete}`;\n                    }\n                    else {\n                        message = `'${wrongText}' {noValidPosition}`;\n                    }\n                    if (expectedText.length > 0) {\n                        message += `{expecting}${expectedText}`;\n                    }\n                }\n                else {\n                    message = msg;\n                }\n            }\n        }\n        else {\n            // No offending symbol, which indicates this is a lexer error.\n            if (e instanceof antlr4ng__rspack_import_0.LexerNoViableAltException) {\n                const lexer = recognizer;\n                const input = lexer.inputStream;\n                let text = lexer.getErrorDisplay(input.getText(lexer._tokenStartCharIndex, input.index));\n                switch (text[0]) {\n                    case '/':\n                        message = '{unfinishedMultilineComment}';\n                        break;\n                    case '\"':\n                        message = '{unfinishedDoubleQuoted}';\n                        break;\n                    case \"'\":\n                        message = '{unfinishedSingleQuoted}';\n                        break;\n                    case '`':\n                        message = '{unfinishedTickQuoted}';\n                        break;\n                    default:\n                        message = '\"' + text + '\" {noValidInput}';\n                        break;\n                }\n            }\n        }\n        message = (0,_transform__rspack_import_1.transform)(message, this.locale);\n        let endCol = charPositionInLine + 1;\n        if (offendingSymbol && offendingSymbol.text !== null) {\n            endCol = charPositionInLine + offendingSymbol.text.length;\n        }\n        if (this._errorListener) {\n            this._errorListener({\n                startLine: line,\n                endLine: line,\n                startColumn: charPositionInLine + 1,\n                endColumn: endCol + 1,\n                message,\n            }, {\n                e,\n                line,\n                msg,\n                recognizer,\n                offendingSymbol,\n                charPositionInLine,\n            });\n        }\n    }\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/parseErrorListener.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/semanticContextCollector.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  \"default\": () => (__rspack_default_export)\n});\n/* import */ var antlr4ng__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/antlr4ng@2.0.11_antlr4ng-cli@1.0.7/node_modules/antlr4ng/dist/index.mjs\");\n/* import */ var _common_findCaretTokenIndex__rspack_import_2 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/findCaretTokenIndex.js\");\n/* import */ var _common_types__rspack_import_1 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/types.js\");\n/* import */ var _basicSQL__rspack_import_3 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/basicSQL.js\");\n\n\n\n\nclass SemanticContextCollector {\n    constructor(_input, caretPosition, allTokens, options) {\n        this.options = {\n            sqlSplitStrategy: _common_types__rspack_import_1.SqlSplitStrategy.STRICT,\n        };\n        this._allTokens = [];\n        /**\n         * If current caret position is in a beginning of statement semantics, it needs to follow some cases:\n         * @case1 there is no statement node with an error before the current statement in the parse tree;\n         *\n         * @case2 if it is an uncomplete keyword, it will be parsed as an `ErrorNode`\n         * and need be a direct child node of `program`;\n         *\n         * @case3 if it is a complete keyword, the parsed TerminalNode or ErrorNode should be\n         * the first leaf node of current statement rule;\n         *\n         * @case4 if it is whiteSpace in caret position, we can't visit it in antlr4 listener,\n         * so we find the first unhidden token before the whiteSpace token, and the unhidden token\n         * should be the last leaf node of statement its belongs to;\n         *\n         * @case5 if the previous token is split symbol like `;`, ignore case1 and forcefully judged as beginning of statement.\n         */\n        this._isStatementBeginning = false;\n        // If caretPosition token is whiteSpace, tokenIndex may be undefined.\n        const tokenIndex = (0,_common_findCaretTokenIndex__rspack_import_2.findCaretTokenIndex)(caretPosition, allTokens);\n        if (tokenIndex !== undefined) {\n            this._tokenIndex = tokenIndex;\n        }\n        this._allTokens = allTokens;\n        this.options = Object.assign(Object.assign({}, this.options), options);\n        if (allTokens === null || allTokens === void 0 ? void 0 : allTokens.length) {\n            let i = tokenIndex ? tokenIndex - 1 : allTokens.length - 1;\n            /**\n             * Link to @case4 and @case5\n             * Find the previous unhidden token.\n             * If can't find tokenIndex or current token is whiteSpace at caretPosition,\n             * prevTokenIndex is useful to help us determine if it is beginning of statement.\n             */\n            while (i >= 0) {\n                if (allTokens[i].channel !== antlr4ng__rspack_import_0.Token.HIDDEN_CHANNEL &&\n                    (allTokens[i].line < caretPosition.lineNumber ||\n                        (allTokens[i].line === caretPosition.lineNumber &&\n                            allTokens[i].column < caretPosition.column))) {\n                    this._prevTokenIndex = allTokens[i].tokenIndex;\n                    break;\n                }\n                i--;\n            }\n            /**\n             * We can directly conclude beginning of statement semantics when current token is\n             * the first token of tokenStream or the previous token is semicolon\n             */\n            if (tokenIndex === 0 ||\n                i === -1 ||\n                (this._prevTokenIndex &&\n                    this._allTokens[this._prevTokenIndex].text === _basicSQL__rspack_import_3.SQL_SPLIT_SYMBOL_TEXT)) {\n                this._isStatementBeginning = true;\n            }\n        }\n    }\n    get semanticContext() {\n        return {\n            isStatementBeginning: this._isStatementBeginning,\n        };\n    }\n    prevStatementHasError(node) {\n        let parent = node.parent;\n        if (!parent)\n            return false;\n        const currentNodeIndex = parent.children.findIndex((child) => child === node);\n        if (currentNodeIndex <= 0)\n            return false;\n        for (let i = currentNodeIndex - 1; i >= 0; i--) {\n            const prevNode = parent.children[i];\n            if (prevNode instanceof antlr4ng__rspack_import_0.ErrorNode ||\n                (prevNode instanceof antlr4ng__rspack_import_0.ParserRuleContext && prevNode.exception !== null))\n                return true;\n        }\n        return false;\n    }\n    /**\n     * Most root rule is `program`.\n     */\n    isRootRule(node) {\n        return node instanceof antlr4ng__rspack_import_0.ParserRuleContext && (node === null || node === void 0 ? void 0 : node.parent) === null;\n    }\n    /**\n     * link to @case4\n     * It should be called in each language's own `enterStatement`.\n     */\n    visitStatement(ctx) {\n        var _a, _b, _c;\n        if (this.options.sqlSplitStrategy === _common_types__rspack_import_1.SqlSplitStrategy.STRICT)\n            return;\n        const isWhiteSpaceToken = this._tokenIndex === undefined ||\n            ((_a = this._allTokens[this._tokenIndex]) === null || _a === void 0 ? void 0 : _a.type) === this.getWhiteSpaceRuleType() ||\n            // PostgreSQL whiteSpace not inlcudes '\\n' symbol\n            ((_b = this._allTokens[this._tokenIndex]) === null || _b === void 0 ? void 0 : _b.text) === '\\n';\n        const isPrevTokenEndOfStatement = this._prevTokenIndex && ((_c = ctx.stop) === null || _c === void 0 ? void 0 : _c.tokenIndex) === this._prevTokenIndex;\n        if (isWhiteSpaceToken && isPrevTokenEndOfStatement && ctx.exception === null) {\n            this._isStatementBeginning = !this.prevStatementHasError(ctx)\n                ? true\n                : this._isStatementBeginning;\n        }\n    }\n    /**\n     * Uncomplete keyword will be error node\n     */\n    visitErrorNode(node) {\n        var _a, _b, _c, _d;\n        if (node.symbol.tokenIndex !== this._tokenIndex ||\n            this._isStatementBeginning ||\n            this.options.sqlSplitStrategy === _common_types__rspack_import_1.SqlSplitStrategy.STRICT)\n            return;\n        let parent = node.parent;\n        let currentNode = node;\n        /**\n         * Link to @case2\n         * The error node is a direct child node of the program node\n         */\n        if (this.isRootRule(parent)) {\n            this._isStatementBeginning = !this.prevStatementHasError(currentNode);\n            return;\n        }\n        /**\n         * Link to @case3\n         * Error node must be the first leaf node of the statement parse tree.\n         **/\n        while (parent !== null && parent.ruleIndex !== this.getStatementRuleType()) {\n            if (((_a = parent.children) === null || _a === void 0 ? void 0 : _a[0]) !== currentNode) {\n                this._isStatementBeginning = false;\n                return;\n            }\n            currentNode = parent;\n            parent = currentNode.parent;\n        }\n        let isStatementBeginning = true;\n        /**\n         * Link to @case1\n         * Previous statement must have no exception\n         */\n        if ((parent === null || parent === void 0 ? void 0 : parent.ruleIndex) === this.getStatementRuleType()) {\n            const programRule = parent.parent;\n            const currentStatementRuleIndex = ((_b = programRule === null || programRule === void 0 ? void 0 : programRule.children) === null || _b === void 0 ? void 0 : _b.findIndex((node) => node === parent)) || -1;\n            if (currentStatementRuleIndex > 0) {\n                /**\n                 * When you typed a keyword and doesn't match any rule, you will get a EOF error,\n                 * For example, just typed 'CREATE', 'INSERT'.\n                 */\n                const isStatementEOF = ((_d = (_c = parent.exception) === null || _c === void 0 ? void 0 : _c.offendingToken) === null || _d === void 0 ? void 0 : _d.text) === '<EOF>';\n                isStatementBeginning =\n                    this.prevStatementHasError(parent) && !isStatementEOF\n                        ? false\n                        : isStatementBeginning;\n            }\n        }\n        this._isStatementBeginning = isStatementBeginning;\n    }\n    visitTerminal(node) {\n        var _a, _b;\n        if (node.symbol.tokenIndex !== this._tokenIndex ||\n            this._isStatementBeginning ||\n            this.options.sqlSplitStrategy === _common_types__rspack_import_1.SqlSplitStrategy.STRICT)\n            return;\n        let currentNode = node;\n        let parent = node.parent;\n        /**\n         * Link to @case3\n         * Current terminal node must be the first leaf node of the statement parse tree.\n         **/\n        while (parent !== null && parent.ruleIndex !== this.getStatementRuleType()) {\n            if (((_a = parent.children) === null || _a === void 0 ? void 0 : _a[0]) !== currentNode) {\n                this._isStatementBeginning = false;\n                return;\n            }\n            currentNode = parent;\n            parent = currentNode.parent;\n        }\n        let isStatementBeginning = true;\n        /**\n         * Link to @case1\n         * Previous statement must have no exception\n         */\n        if ((parent === null || parent === void 0 ? void 0 : parent.ruleIndex) === this.getStatementRuleType()) {\n            const programRule = parent.parent;\n            const currentStatementRuleIndex = ((_b = programRule === null || programRule === void 0 ? void 0 : programRule.children) === null || _b === void 0 ? void 0 : _b.findIndex((node) => node === parent)) || -1;\n            if (currentStatementRuleIndex > 0) {\n                isStatementBeginning = this.prevStatementHasError(parent)\n                    ? false\n                    : isStatementBeginning;\n            }\n        }\n        this._isStatementBeginning = isStatementBeginning;\n    }\n    enterEveryRule(_node) { }\n    exitEveryRule(_node) { }\n}\n/* export default */ const __rspack_default_export = (SemanticContextCollector);\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/semanticContextCollector.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/splitListener.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  SplitListener: () => (SplitListener)\n});\nclass SplitListener {\n    constructor() {\n        this._statementsContext = [];\n    }\n    visitTerminal() { }\n    visitErrorNode() { }\n    enterEveryRule() { }\n    exitEveryRule() { }\n    get statementsContext() {\n        return this._statementsContext;\n    }\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/splitListener.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/tokenUtils.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  processTokenCandidates: () => (processTokenCandidates)\n});\n/**\n * Utility function for processing SQL tokens and generating keyword suggestions\n */\n/**\n * Process token candidates and generate a list of keyword suggestions\n * @param parser SQL parser instance\n * @param tokens token candidates\n * @returns list of keyword suggestions\n */\nfunction processTokenCandidates(parser, tokens) {\n    const keywords = [];\n    const cleanDisplayName = (displayName) => {\n        return displayName && displayName.startsWith(\"'\") && displayName.endsWith(\"'\")\n            ? displayName.slice(1, -1)\n            : displayName || '';\n    };\n    const isKeywordToken = (token) => {\n        const symbolicName = parser.vocabulary.getSymbolicName(token);\n        return Boolean(symbolicName === null || symbolicName === void 0 ? void 0 : symbolicName.startsWith('KW_'));\n    };\n    for (const [token, followSets] of tokens) {\n        const displayName = parser.vocabulary.getDisplayName(token);\n        if (!displayName || !isKeywordToken(token))\n            continue;\n        const keyword = cleanDisplayName(displayName);\n        keywords.push(keyword);\n        if (followSets.length && followSets.every((s) => isKeywordToken(s))) {\n            const followKeywords = followSets\n                .map((s) => cleanDisplayName(parser.vocabulary.getDisplayName(s)))\n                .filter(Boolean);\n            if (followKeywords.length) {\n                const combinedKeyword = [keyword, ...followKeywords].join(' ');\n                keywords.push(combinedKeyword);\n            }\n        }\n    }\n    return keywords;\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/tokenUtils.js\n}");

},
"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/transform.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  transform: () => (transform)\n});\n/* import */ var _locale_locale__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/locale/locale.js\");\n\n/**\n * transform message to locale language\n * @param message error msg\n * @param locale language setting\n */\nfunction transform(message, locale) {\n    const regex = /{([^}]+)}/g;\n    return message.replace(regex, (_, key) => _locale_locale__rspack_import_0.i18n[locale][key] || '');\n}\n\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/dt-sql-parser@4.5.0_antlr4ng-cli@1.0.7/node_modules/dt-sql-parser/dist/parser/common/transform.js\n}");

},
"./node_modules/.pnpm/monaco-editor@0.52.2/node_modules/monaco-editor/esm/vs/editor/editor.worker.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  initialize: () => (initialize)\n});\n/* import */ var _base_common_worker_simpleWorker_js__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/monaco-editor@0.52.2/node_modules/monaco-editor/esm/vs/base/common/worker/simpleWorker.js\");\n/* import */ var _common_services_editorSimpleWorker_js__rspack_import_1 = __webpack_require__(\"./node_modules/.pnpm/monaco-editor@0.52.2/node_modules/monaco-editor/esm/vs/editor/common/services/editorSimpleWorker.js\");\n/* import */ var _common_services_editorWorkerHost_js__rspack_import_2 = __webpack_require__(\"./node_modules/.pnpm/monaco-editor@0.52.2/node_modules/monaco-editor/esm/vs/editor/common/services/editorWorkerHost.js\");\n/*---------------------------------------------------------------------------------------------\n *  Copyright (c) Microsoft Corporation. All rights reserved.\n *  Licensed under the MIT License. See License.txt in the project root for license information.\n *--------------------------------------------------------------------------------------------*/\n\n\n\nlet initialized = false;\nfunction initialize(foreignModule) {\n    if (initialized) {\n        return;\n    }\n    initialized = true;\n    const simpleWorker = new _base_common_worker_simpleWorker_js__rspack_import_0.SimpleWorkerServer((msg) => {\n        globalThis.postMessage(msg);\n    }, (workerServer) => new _common_services_editorSimpleWorker_js__rspack_import_1.EditorSimpleWorker(_common_services_editorWorkerHost_js__rspack_import_2.EditorWorkerHost.getChannel(workerServer), foreignModule));\n    globalThis.onmessage = (e) => {\n        simpleWorker.onmessage(e.data);\n    };\n}\nglobalThis.onmessage = (e) => {\n    // Ignore first message in this case and initialize if not yet initialized\n    if (!initialized) {\n        initialize(null);\n    }\n};\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/monaco-editor@0.52.2/node_modules/monaco-editor/esm/vs/editor/editor.worker.js\n}");

},
"./node_modules/.pnpm/monaco-sql-languages@1.1.0_antlr4ng-cli@1.0.7_monaco-editor@0.52.2/node_modules/monaco-sql-languages/esm/baseSQLWorker.js"(__unused_rspack_module, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  BaseSQLWorker: () => (BaseSQLWorker)\n});\nvar __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {\n    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }\n    return new (P || (P = Promise))(function (resolve, reject) {\n        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }\n        function rejected(value) { try { step(generator[\"throw\"](value)); } catch (e) { reject(e); } }\n        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }\n        step((generator = generator.apply(thisArg, _arguments || [])).next());\n    });\n};\nclass BaseSQLWorker {\n    constructor(_ctx, _createData) {\n        this.keywords = [];\n    }\n    doValidation(code) {\n        return __awaiter(this, void 0, void 0, function* () {\n            code = code || this.getTextDocument();\n            if (code) {\n                const result = this.parser.validate(code);\n                return Promise.resolve(result);\n            }\n            return Promise.resolve([]);\n        });\n    }\n    getSerializedParseTree(code) {\n        return __awaiter(this, void 0, void 0, function* () {\n            if (!code)\n                return Promise.resolve(null);\n            const parser = this.parser.createParser(code);\n            const parseTree = parser.program();\n            const ruleNames = parser.ruleNames;\n            const tokenTypeMap = parser.getTokenTypeMap();\n            const tokenNameMap = new Map();\n            // Revert map key and value\n            tokenTypeMap.forEach((tokenType, name) => {\n                tokenNameMap.set(tokenType, name);\n            });\n            function serializeNode(node) {\n                var _a;\n                if (!node)\n                    return null;\n                const isRuleNode = !node.symbol;\n                const serializedNode = {\n                    ruleName: isRuleNode ? ruleNames[node.ruleIndex] : node.constructor.name,\n                    text: isRuleNode\n                        ? ''\n                        : tokenNameMap.get((_a = node.symbol.tokenSource) === null || _a === void 0 ? void 0 : _a.type) + ': ' + node.symbol.text,\n                    children: []\n                };\n                for (let i = 0; i < node.getChildCount(); i++) {\n                    const child = node.getChild(i);\n                    if (child) {\n                        serializedNode.children.push(serializeNode(child));\n                    }\n                }\n                return serializedNode;\n            }\n            return Promise.resolve(serializeNode(parseTree));\n        });\n    }\n    doCompletion(code, position) {\n        return __awaiter(this, void 0, void 0, function* () {\n            code = code || this.getTextDocument();\n            if (code) {\n                const suggestions = this.parser.getSuggestionAtCaretPosition(code, position);\n                return Promise.resolve(suggestions);\n            }\n            return Promise.resolve(null);\n        });\n    }\n    doCompletionWithEntities(code, position) {\n        return __awaiter(this, void 0, void 0, function* () {\n            var _a;\n            code = code || this.getTextDocument();\n            if (code) {\n                const suggestions = this.parser.getSuggestionAtCaretPosition(code, position);\n                let allEntities = null;\n                if ((_a = suggestions === null || suggestions === void 0 ? void 0 : suggestions.syntax) === null || _a === void 0 ? void 0 : _a.length) {\n                    allEntities = this.parser.getAllEntities(code, position);\n                }\n                const semanticContext = this.parser.getSemanticContextAtCaretPosition(code, position);\n                return Promise.resolve({\n                    suggestions,\n                    allEntities,\n                    context: semanticContext\n                });\n            }\n            return Promise.resolve({\n                suggestions: null,\n                allEntities: null,\n                context: null\n            });\n        });\n    }\n    getAllEntities(code, position) {\n        return __awaiter(this, void 0, void 0, function* () {\n            code = code || this.getTextDocument();\n            if (code) {\n                const allEntities = this.parser.getAllEntities(code, position);\n                return Promise.resolve(allEntities);\n            }\n            return Promise.resolve(null);\n        });\n    }\n    getTextDocument() {\n        const model = this._ctx.getMirrorModels()[0]; // When there are multiple files open, this will be an array\n        return model && model.getValue();\n    }\n}\n\n\n//# sourceURL=Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/monaco-sql-languages@1.1.0_antlr4ng-cli@1.0.7_monaco-editor@0.52.2/node_modules/monaco-sql-languages/esm/baseSQLWorker.js\n}");

},
"./node_modules/.pnpm/antlr4-c3@3.3.7_antlr4ng-cli@1.0.7/node_modules/antlr4-c3/lib/index.mjs"(__unused_rspack___webpack_module__, __webpack_exports__, __webpack_require__) {
eval("{__webpack_require__.r(__webpack_exports__);\n__webpack_require__.d(__webpack_exports__, {\n  ArrayType: () => (ArrayType),\n  BaseSymbol: () => (BaseSymbol),\n  BlockSymbol: () => (BlockSymbol),\n  CandidatesCollection: () => (CandidatesCollection),\n  ClassSymbol: () => (ClassSymbol),\n  CodeCompletionCore: () => (CodeCompletionCore),\n  DuplicateSymbolError: () => (DuplicateSymbolError),\n  FieldSymbol: () => (FieldSymbol),\n  FundamentalType: () => (FundamentalType),\n  InterfaceSymbol: () => (InterfaceSymbol),\n  LiteralSymbol: () => (LiteralSymbol),\n  MemberVisibility: () => (MemberVisibility),\n  MethodFlags: () => (MethodFlags),\n  MethodSymbol: () => (MethodSymbol),\n  Modifier: () => (Modifier),\n  NamespaceSymbol: () => (NamespaceSymbol),\n  ParameterSymbol: () => (ParameterSymbol),\n  ReferenceKind: () => (ReferenceKind),\n  RoutineSymbol: () => (RoutineSymbol),\n  ScopedSymbol: () => (ScopedSymbol),\n  SymbolTable: () => (SymbolTable),\n  TypeAlias: () => (TypeAlias),\n  TypeKind: () => (TypeKind),\n  TypedSymbol: () => (TypedSymbol),\n  VariableSymbol: () => (VariableSymbol)\n});\n/* import */ var antlr4ng__rspack_import_0 = __webpack_require__(\"./node_modules/.pnpm/antlr4ng@2.0.11_antlr4ng-cli@1.0.7/node_modules/antlr4ng/dist/index.mjs\");\nvar __defProp = Object.defineProperty;\nvar __name = (target, value) => __defProp(target, \"name\", { value, configurable: true });\n\n// src/types.ts\nvar MemberVisibility = /* @__PURE__ */ ((MemberVisibility2) => {\n  MemberVisibility2[MemberVisibility2[\"Unknown\"] = 0] = \"Unknown\";\n  MemberVisibility2[MemberVisibility2[\"Open\"] = 1] = \"Open\";\n  MemberVisibility2[MemberVisibility2[\"Public\"] = 2] = \"Public\";\n  MemberVisibility2[MemberVisibility2[\"Protected\"] = 3] = \"Protected\";\n  MemberVisibility2[MemberVisibility2[\"Private\"] = 4] = \"Private\";\n  MemberVisibility2[MemberVisibility2[\"FilePrivate\"] = 5] = \"FilePrivate\";\n  MemberVisibility2[MemberVisibility2[\"Library\"] = 6] = \"Library\";\n  return MemberVisibility2;\n})(MemberVisibility || {});\nvar Modifier = /* @__PURE__ */ ((Modifier3) => {\n  Modifier3[Modifier3[\"Static\"] = 0] = \"Static\";\n  Modifier3[Modifier3[\"Final\"] = 1] = \"Final\";\n  Modifier3[Modifier3[\"Sealed\"] = 2] = \"Sealed\";\n  Modifier3[Modifier3[\"Abstract\"] = 3] = \"Abstract\";\n  Modifier3[Modifier3[\"Deprecated\"] = 4] = \"Deprecated\";\n  Modifier3[Modifier3[\"Virtual\"] = 5] = \"Virtual\";\n  Modifier3[Modifier3[\"Const\"] = 6] = \"Const\";\n  Modifier3[Modifier3[\"Overwritten\"] = 7] = \"Overwritten\";\n  return Modifier3;\n})(Modifier || {});\nvar TypeKind = /* @__PURE__ */ ((TypeKind2) => {\n  TypeKind2[TypeKind2[\"Unknown\"] = 0] = \"Unknown\";\n  TypeKind2[TypeKind2[\"Integer\"] = 1] = \"Integer\";\n  TypeKind2[TypeKind2[\"Float\"] = 2] = \"Float\";\n  TypeKind2[TypeKind2[\"Number\"] = 3] = \"Number\";\n  TypeKind2[TypeKind2[\"String\"] = 4] = \"String\";\n  TypeKind2[TypeKind2[\"Char\"] = 5] = \"Char\";\n  TypeKind2[TypeKind2[\"Boolean\"] = 6] = \"Boolean\";\n  TypeKind2[TypeKind2[\"Class\"] = 7] = \"Class\";\n  TypeKind2[TypeKind2[\"Interface\"] = 8] = \"Interface\";\n  TypeKind2[TypeKind2[\"Array\"] = 9] = \"Array\";\n  TypeKind2[TypeKind2[\"Map\"] = 10] = \"Map\";\n  TypeKind2[TypeKind2[\"Enum\"] = 11] = \"Enum\";\n  TypeKind2[TypeKind2[\"Alias\"] = 12] = \"Alias\";\n  return TypeKind2;\n})(TypeKind || {});\nvar ReferenceKind = /* @__PURE__ */ ((ReferenceKind3) => {\n  ReferenceKind3[ReferenceKind3[\"Irrelevant\"] = 0] = \"Irrelevant\";\n  ReferenceKind3[ReferenceKind3[\"Pointer\"] = 1] = \"Pointer\";\n  ReferenceKind3[ReferenceKind3[\"Reference\"] = 2] = \"Reference\";\n  ReferenceKind3[ReferenceKind3[\"Instance\"] = 3] = \"Instance\";\n  return ReferenceKind3;\n})(ReferenceKind || {});\n\n// src/BaseSymbol.ts\nvar BaseSymbol = class {\n  static {\n    __name(this, \"BaseSymbol\");\n  }\n  /** The name of the symbol or empty if anonymous. */\n  name;\n  /** Reference to the parse tree which contains this symbol. */\n  context;\n  modifiers = /* @__PURE__ */ new Set();\n  visibility = 0 /* Unknown */;\n  #parent;\n  constructor(name = \"\") {\n    this.name = name;\n  }\n  get parent() {\n    return this.#parent;\n  }\n  get firstSibling() {\n    if (!this.#parent) {\n      return void 0;\n    }\n    return this.#parent?.firstChild;\n  }\n  /**\n   * @returns the symbol before this symbol in its scope.\n   */\n  get previousSibling() {\n    if (!this.#parent) {\n      return void 0;\n    }\n    if (!this.#parent) {\n      return this;\n    }\n    return this.#parent.previousSiblingOf(this);\n  }\n  /**\n   * @returns the symbol following this symbol in its scope.\n   */\n  get nextSibling() {\n    return this.#parent?.nextSiblingOf(this);\n  }\n  get lastSibling() {\n    return this.#parent?.lastChild;\n  }\n  /**\n   * @returns the next symbol in definition order, regardless of the scope.\n   */\n  get next() {\n    return this.#parent?.nextOf(this);\n  }\n  /**\n   * @returns the outermost entity (below the symbol table) that holds us.\n   */\n  get root() {\n    let run = this.#parent;\n    while (run) {\n      if (!run.parent || this.isSymbolTable(run.parent)) {\n        return run;\n      }\n      run = run.parent;\n    }\n    return run;\n  }\n  /**\n   * @returns the symbol table we belong too or undefined if we are not yet assigned.\n   */\n  get symbolTable() {\n    if (this.isSymbolTable(this)) {\n      return this;\n    }\n    let run = this.#parent;\n    while (run) {\n      if (this.isSymbolTable(run)) {\n        return run;\n      }\n      run = run.parent;\n    }\n    return void 0;\n  }\n  /**\n   * @returns the list of symbols from this one up to root.\n   */\n  get symbolPath() {\n    const result = [];\n    let run = this;\n    while (run) {\n      result.push(run);\n      if (!run.parent) {\n        break;\n      }\n      run = run.parent;\n    }\n    return result;\n  }\n  /**\n   * This is rather an internal method and should rarely be used by external code.\n   *\n   * @param parent The new parent to use.\n   */\n  setParent(parent) {\n    this.#parent = parent;\n  }\n  /**\n   * Remove this symbol from its parent scope.\n   */\n  removeFromParent() {\n    this.#parent?.removeSymbol(this);\n    this.#parent = void 0;\n  }\n  /**\n   * Asynchronously looks up a symbol with a given name, in a bottom-up manner.\n   *\n   * @param name The name of the symbol to find.\n   * @param localOnly If true only child symbols are returned, otherwise also symbols from the parent of this symbol\n   *                  (recursively).\n   *\n   * @returns A promise resolving to the first symbol with a given name, in the order of appearance in this scope\n   *          or any of the parent scopes (conditionally).\n   */\n  async resolve(name, localOnly = false) {\n    return this.#parent?.resolve(name, localOnly);\n  }\n  /**\n   * Synchronously looks up a symbol with a given name, in a bottom-up manner.\n   *\n   * @param name The name of the symbol to find.\n   * @param localOnly If true only child symbols are returned, otherwise also symbols from the parent of this symbol\n   *                  (recursively).\n   *\n   * @returns the first symbol with a given name, in the order of appearance in this scope\n   *          or any of the parent scopes (conditionally).\n   */\n  resolveSync(name, localOnly = false) {\n    return this.#parent?.resolveSync(name, localOnly);\n  }\n  /**\n   * @param t The type of objects to return.\n   *\n   * @returns the next enclosing parent of the given type.\n   */\n  getParentOfType(t) {\n    let run = this.#parent;\n    while (run) {\n      if (run instanceof t) {\n        return run;\n      }\n      run = run.parent;\n    }\n    return void 0;\n  }\n  /**\n   * Creates a qualified identifier from this symbol and its parent.\n   * If `full` is true then all parents are traversed in addition to this instance.\n   *\n   * @param separator The string to be used between the parts.\n   * @param full A flag indicating if the full path is to be returned.\n   * @param includeAnonymous Use a special string for empty scope names.\n   *\n   * @returns the constructed qualified identifier.\n   */\n  qualifiedName(separator = \".\", full = false, includeAnonymous = false) {\n    if (!includeAnonymous && this.name.length === 0) {\n      return \"\";\n    }\n    let result = this.name.length === 0 ? \"<anonymous>\" : this.name;\n    let run = this.#parent;\n    while (run) {\n      if (includeAnonymous || run.name.length > 0) {\n        result = (run.name.length === 0 ? \"<anonymous>\" : run.name) + separator + result;\n      }\n      if (!full || !run.parent) {\n        break;\n      }\n      run = run.parent;\n    }\n    return result;\n  }\n  /**\n   * Type guard to check for ISymbolTable.\n   *\n   * @param candidate The object to check.\n   *\n   * @returns true if the object is a symbol table.\n   */\n  isSymbolTable(candidate) {\n    return candidate.info !== void 0;\n  }\n};\n\n// src/ArrayType.ts\nvar ArrayType = class extends BaseSymbol {\n  static {\n    __name(this, \"ArrayType\");\n  }\n  elementType;\n  size;\n  // > 0 if fixed length.\n  referenceKind;\n  constructor(name, referenceKind, elemType, size = 0) {\n    super(name);\n    this.referenceKind = referenceKind;\n    this.elementType = elemType;\n    this.size = size;\n  }\n  get baseTypes() {\n    return [];\n  }\n  get kind() {\n    return 9 /* Array */;\n  }\n  get reference() {\n    return this.referenceKind;\n  }\n};\n\n// src/DuplicateSymbolError.ts\nvar DuplicateSymbolError = class extends Error {\n  static {\n    __name(this, \"DuplicateSymbolError\");\n  }\n};\n\n// src/ScopedSymbol.ts\nvar ScopedSymbol = class _ScopedSymbol extends BaseSymbol {\n  static {\n    __name(this, \"ScopedSymbol\");\n  }\n  /** All child symbols in definition order. */\n  #children = [];\n  // All used child names. Used to detect name collisions.\n  #names = /* @__PURE__ */ new Map();\n  constructor(name = \"\") {\n    super(name);\n  }\n  /**\n   * @returns A promise resolving to all direct child symbols with a scope (e.g. classes in a module).\n   */\n  get directScopes() {\n    return this.getSymbolsOfType(_ScopedSymbol);\n  }\n  get children() {\n    return this.#children;\n  }\n  get firstChild() {\n    if (this.#children.length > 0) {\n      return this.#children[0];\n    }\n    return void 0;\n  }\n  get lastChild() {\n    if (this.#children.length > 0) {\n      return this.#children[this.#children.length - 1];\n    }\n    return void 0;\n  }\n  clear() {\n    this.#children = [];\n    this.#names.clear();\n  }\n  /**\n   * Adds the given symbol to this scope. If it belongs already to a different scope\n   * it is removed from that before adding it here.\n   *\n   * @param symbol The symbol to add as a child.\n   */\n  addSymbol(symbol) {\n    symbol.removeFromParent();\n    const symbolTable = this.symbolTable;\n    const count = this.#names.get(symbol.name);\n    if (!symbolTable || !symbolTable.options.allowDuplicateSymbols) {\n      if (count !== void 0) {\n        throw new DuplicateSymbolError(\"Attempt to add duplicate symbol '\" + (symbol.name ?? \"<anonymous>\") + \"'\");\n      } else {\n        this.#names.set(symbol.name, 1);\n      }\n      const index = this.#children.indexOf(symbol);\n      if (index > -1) {\n        throw new DuplicateSymbolError(\"Attempt to add duplicate symbol '\" + (symbol.name ?? \"<anonymous>\") + \"'\");\n      }\n    } else {\n      this.#names.set(symbol.name, count === void 0 ? 1 : count + 1);\n    }\n    this.#children.push(symbol);\n    symbol.setParent(this);\n  }\n  removeSymbol(symbol) {\n    const index = this.#children.indexOf(symbol);\n    if (index > -1) {\n      this.#children.splice(index, 1);\n      symbol.setParent(void 0);\n      const count = this.#names.get(symbol.name);\n      if (count !== void 0) {\n        if (count === 1) {\n          this.#names.delete(symbol.name);\n        } else {\n          this.#names.set(symbol.name, count - 1);\n        }\n      }\n    }\n  }\n  /**\n   * Asynchronously retrieves child symbols of a given type from this symbol.\n   *\n   * @param t The type of of the objects to return.\n   *\n   * @returns A promise resolving to all (nested) children of the given type.\n   */\n  async getNestedSymbolsOfType(t) {\n    const result = [];\n    const childPromises = [];\n    this.#children.forEach((child) => {\n      if (child instanceof t) {\n        result.push(child);\n      }\n      if (child instanceof _ScopedSymbol) {\n        childPromises.push(child.getNestedSymbolsOfType(t));\n      }\n    });\n    const childSymbols = await Promise.all(childPromises);\n    childSymbols.forEach((entry) => {\n      result.push(...entry);\n    });\n    return result;\n  }\n  /**\n   * Synchronously retrieves child symbols of a given type from this symbol.\n   *\n   * @param t The type of of the objects to return.\n   *\n   * @returns A list of all (nested) children of the given type.\n   */\n  getNestedSymbolsOfTypeSync(t) {\n    const result = [];\n    this.#children.forEach((child) => {\n      if (child instanceof t) {\n        result.push(child);\n      }\n      if (child instanceof _ScopedSymbol) {\n        result.push(...child.getNestedSymbolsOfTypeSync(t));\n      }\n    });\n    return result;\n  }\n  /**\n   * @param name If given only returns symbols with that name.\n   *\n   * @returns A promise resolving to symbols from this and all nested scopes in the order they were defined.\n   */\n  async getAllNestedSymbols(name) {\n    const result = [];\n    const childPromises = [];\n    this.#children.forEach((child) => {\n      if (!name || child.name === name) {\n        result.push(child);\n      }\n      if (child instanceof _ScopedSymbol) {\n        childPromises.push(child.getAllNestedSymbols(name));\n      }\n    });\n    const childSymbols = await Promise.all(childPromises);\n    childSymbols.forEach((entry) => {\n      result.push(...entry);\n    });\n    return result;\n  }\n  /**\n   * @param name If given only returns symbols with that name.\n   *\n   * @returns A list of all symbols from this and all nested scopes in the order they were defined.\n   */\n  getAllNestedSymbolsSync(name) {\n    const result = [];\n    this.#children.forEach((child) => {\n      if (!name || child.name === name) {\n        result.push(child);\n      }\n      if (child instanceof _ScopedSymbol) {\n        result.push(...child.getAllNestedSymbolsSync(name));\n      }\n    });\n    return result;\n  }\n  /**\n   * @param t The type of of the objects to return.\n   *\n   * @returns A promise resolving to direct children of a given type.\n   */\n  getSymbolsOfType(t) {\n    return new Promise((resolve) => {\n      const result = [];\n      this.#children.forEach((child) => {\n        if (child instanceof t) {\n          result.push(child);\n        }\n      });\n      resolve(result);\n    });\n  }\n  /**\n   * TODO: add optional position dependency (only symbols defined before a given caret pos are viable).\n   *\n   * @param t The type of the objects to return.\n   * @param localOnly If true only child symbols are returned, otherwise also symbols from the parent of this symbol\n   *                  (recursively).\n   *\n   * @returns A promise resolving to all symbols of the the given type, accessible from this scope (if localOnly is\n   *          false), within the owning symbol table.\n   */\n  async getAllSymbols(t, localOnly = false) {\n    const result = [];\n    for (const child of this.#children) {\n      if (child instanceof t) {\n        result.push(child);\n      }\n      if (this.isNamespace(child)) {\n        const childSymbols = await child.getAllSymbols(t, true);\n        result.push(...childSymbols);\n      }\n    }\n    if (!localOnly) {\n      if (this.parent) {\n        const childSymbols = await this.getAllSymbols(t, true);\n        result.push(...childSymbols);\n      }\n    }\n    return result;\n  }\n  /**\n   * TODO: add optional position dependency (only symbols defined before a given caret pos are viable).\n   *\n   * @param t The type of the objects to return.\n   * @param localOnly If true only child symbols are returned, otherwise also symbols from the parent of this symbol\n   *                  (recursively).\n   *\n   * @returns A list with all symbols of the the given type, accessible from this scope (if localOnly is\n   *          false), within the owning symbol table.\n   */\n  getAllSymbolsSync(t, localOnly = false) {\n    const result = [];\n    for (const child of this.#children) {\n      if (child instanceof t) {\n        result.push(child);\n      }\n      if (this.isNamespace(child)) {\n        const childSymbols = child.getAllSymbolsSync(t, true);\n        result.push(...childSymbols);\n      }\n    }\n    if (!localOnly) {\n      if (this.parent) {\n        const childSymbols = this.getAllSymbolsSync(t, true);\n        result.push(...childSymbols);\n      }\n    }\n    return result;\n  }\n  /**\n   * @param name The name of the symbol to resolve.\n   * @param localOnly If true only child symbols are returned, otherwise also symbols from the parent of this symbol\n   *                  (recursively).\n   *\n   * @returns A promise resolving to the first symbol with a given name, in the order of appearance in this scope\n   *          or any of the parent scopes (conditionally).\n   */\n  async resolve(name, localOnly = false) {\n    return new Promise((resolve) => {\n      for (const child of this.#children) {\n        if (child.name === name) {\n          resolve(child);\n          return;\n        }\n      }\n      if (!localOnly) {\n        if (this.parent) {\n          resolve(this.parent.resolve(name, false));\n          return;\n        }\n      }\n      resolve(void 0);\n    });\n  }\n  /**\n   * @param name The name of the symbol to resolve.\n   * @param localOnly If true only child symbols are returned, otherwise also symbols from the parent of this symbol\n   *                  (recursively).\n   *\n   * @returns the first symbol with a given name, in the order of appearance in this scope\n   *          or any of the parent scopes (conditionally).\n   */\n  resolveSync(name, localOnly = false) {\n    for (const child of this.#children) {\n      if (child.name === name) {\n        return child;\n      }\n    }\n    if (!localOnly) {\n      if (this.parent) {\n        return this.parent.resolveSync(name, false);\n      }\n    }\n    return void 0;\n  }\n  /**\n   * @param path The path consisting of symbol names separator by `separator`.\n   * @param separator The character to separate path segments.\n   *\n   * @returns the symbol located at the given path through the symbol hierarchy.\n   */\n  symbolFromPath(path, separator = \".\") {\n    const elements = path.split(separator);\n    let index = 0;\n    if (elements[0] === this.name || elements[0].length === 0) {\n      ++index;\n    }\n    let result = this;\n    while (index < elements.length) {\n      if (!(result instanceof _ScopedSymbol)) {\n        return void 0;\n      }\n      const child = result.children.find((candidate) => {\n        return candidate.name === elements[index];\n      });\n      if (!child) {\n        return void 0;\n      }\n      result = child;\n      ++index;\n    }\n    return result;\n  }\n  /**\n   * @param child The child to search for.\n   *\n   * @returns the index of the given child symbol in the child list or -1 if it couldn't be found.\n   */\n  indexOfChild(child) {\n    return this.#children.findIndex((value) => {\n      return value === child;\n    });\n  }\n  /**\n   * @param child The reference node.\n   *\n   * @returns the sibling symbol after the given child symbol, if one exists.\n   */\n  nextSiblingOf(child) {\n    const index = this.indexOfChild(child);\n    if (index === -1 || index >= this.#children.length - 1) {\n      return void 0;\n    }\n    return this.#children[index + 1];\n  }\n  /**\n   * @param child The reference node.\n   *\n   * @returns the sibling symbol before the given child symbol, if one exists.\n   */\n  previousSiblingOf(child) {\n    const index = this.indexOfChild(child);\n    if (index < 1) {\n      return void 0;\n    }\n    return this.#children[index - 1];\n  }\n  /**\n   * @param child The reference node.\n   *\n   * @returns the next symbol in definition order, regardless of the scope.\n   */\n  nextOf(child) {\n    if (!child.parent) {\n      return void 0;\n    }\n    if (child.parent !== this) {\n      return child.parent.nextOf(child);\n    }\n    if (child instanceof _ScopedSymbol && child.children.length > 0) {\n      return child.children[0];\n    }\n    const sibling = this.nextSiblingOf(child);\n    if (sibling) {\n      return sibling;\n    }\n    return this.parent.nextOf(this);\n  }\n  isNamespace(candidate) {\n    return candidate.inline !== void 0 && candidate.attributes !== void 0;\n  }\n};\n\n// src/BlockSymbol.ts\nvar BlockSymbol = class extends ScopedSymbol {\n  static {\n    __name(this, \"BlockSymbol\");\n  }\n};\n\n// src/TypedSymbol.ts\nvar TypedSymbol = class extends BaseSymbol {\n  static {\n    __name(this, \"TypedSymbol\");\n  }\n  type;\n  constructor(name, type) {\n    super(name);\n    this.type = type;\n  }\n};\n\n// src/VariableSymbol.ts\nvar VariableSymbol = class extends TypedSymbol {\n  static {\n    __name(this, \"VariableSymbol\");\n  }\n  value;\n  constructor(name, value, type) {\n    super(name, type);\n    this.value = value;\n  }\n};\n\n// src/FieldSymbol.ts\nvar FieldSymbol = class extends VariableSymbol {\n  static {\n    __name(this, \"FieldSymbol\");\n  }\n  setter;\n  getter;\n};\n\n// src/ParameterSymbol.ts\nvar ParameterSymbol = class extends VariableSymbol {\n  static {\n    __name(this, \"ParameterSymbol\");\n  }\n};\n\n// src/RoutineSymbol.ts\nvar RoutineSymbol = class extends ScopedSymbol {\n  static {\n    __name(this, \"RoutineSymbol\");\n  }\n  returnType;\n  // Can be null if result is void.\n  constructor(name, returnType) {\n    super(name);\n    this.returnType = returnType;\n  }\n  getVariables(_localOnly = true) {\n    return this.getSymbolsOfType(VariableSymbol);\n  }\n  getParameters(_localOnly = true) {\n    return this.getSymbolsOfType(ParameterSymbol);\n  }\n};\n\n// src/MethodSymbol.ts\nvar MethodFlags = /* @__PURE__ */ ((MethodFlags2) => {\n  MethodFlags2[MethodFlags2[\"None\"] = 0] = \"None\";\n  MethodFlags2[MethodFlags2[\"Virtual\"] = 1] = \"Virtual\";\n  MethodFlags2[MethodFlags2[\"Const\"] = 2] = \"Const\";\n  MethodFlags2[MethodFlags2[\"Overwritten\"] = 4] = \"Overwritten\";\n  MethodFlags2[MethodFlags2[\"SetterOrGetter\"] = 8] = \"SetterOrGetter\";\n  MethodFlags2[MethodFlags2[\"Explicit\"] = 16] = \"Explicit\";\n  return MethodFlags2;\n})(MethodFlags || {});\nvar MethodSymbol = class extends RoutineSymbol {\n  static {\n    __name(this, \"MethodSymbol\");\n  }\n  methodFlags = 0 /* None */;\n};\n\n// src/ClassSymbol.ts\nvar ClassSymbol = class extends ScopedSymbol {\n  static {\n    __name(this, \"ClassSymbol\");\n  }\n  isStruct = false;\n  reference = 0 /* Irrelevant */;\n  /** Usually only one member, unless the language supports multiple inheritance (like C++). */\n  // eslint-disable-next-line no-use-before-define\n  extends;\n  /** Typescript allows a class to implement a class, not only interfaces. */\n  // eslint-disable-next-line no-use-before-define\n  implements;\n  constructor(name, ext, impl) {\n    super(name);\n    this.extends = ext;\n    this.implements = impl;\n  }\n  get baseTypes() {\n    return this.extends;\n  }\n  get kind() {\n    return 7 /* Class */;\n  }\n  /**\n   * @param _includeInherited Not used.\n   *\n   * @returns a list of all methods.\n   */\n  getMethods(_includeInherited = false) {\n    return this.getSymbolsOfType(MethodSymbol);\n  }\n  /**\n   * @param _includeInherited Not used.\n   *\n   * @returns all fields.\n   */\n  getFields(_includeInherited = false) {\n    return this.getSymbolsOfType(FieldSymbol);\n  }\n};\n\n// src/FundamentalType.ts\nvar FundamentalType = class _FundamentalType {\n  static {\n    __name(this, \"FundamentalType\");\n  }\n  static integerType = new _FundamentalType(\"int\", 1 /* Integer */, 3 /* Instance */);\n  static floatType = new _FundamentalType(\"float\", 2 /* Float */, 3 /* Instance */);\n  static stringType = new _FundamentalType(\"string\", 4 /* String */, 3 /* Instance */);\n  static boolType = new _FundamentalType(\"bool\", 6 /* Boolean */, 3 /* Instance */);\n  name;\n  typeKind;\n  referenceKind;\n  constructor(name, typeKind = 0 /* Unknown */, referenceKind = 0 /* Irrelevant */) {\n    this.name = name;\n    this.typeKind = typeKind;\n    this.referenceKind = referenceKind;\n  }\n  get baseTypes() {\n    return [];\n  }\n  get kind() {\n    return this.typeKind;\n  }\n  get reference() {\n    return this.referenceKind;\n  }\n};\n\n// src/InterfaceSymbol.ts\nvar InterfaceSymbol = class extends ScopedSymbol {\n  static {\n    __name(this, \"InterfaceSymbol\");\n  }\n  reference = 0 /* Irrelevant */;\n  /** Typescript allows an interface to extend a class, not only interfaces. */\n  // eslint-disable-next-line no-use-before-define\n  extends;\n  constructor(name, ext) {\n    super(name);\n    this.extends = ext;\n  }\n  get baseTypes() {\n    return this.extends;\n  }\n  get kind() {\n    return 8 /* Interface */;\n  }\n  /**\n   * @param _includeInherited not used\n   *\n   * @returns a list of all methods.\n   */\n  getMethods(_includeInherited = false) {\n    return this.getSymbolsOfType(MethodSymbol);\n  }\n  /**\n   * @param _includeInherited Not used.\n   *\n   * @returns all fields.\n   */\n  getFields(_includeInherited = false) {\n    return this.getSymbolsOfType(FieldSymbol);\n  }\n};\n\n// src/LiteralSymbol.ts\nvar LiteralSymbol = class extends TypedSymbol {\n  static {\n    __name(this, \"LiteralSymbol\");\n  }\n  value;\n  constructor(name, value, type) {\n    super(name, type);\n    this.value = value;\n  }\n};\n\n// src/NamespaceSymbol.ts\nvar NamespaceSymbol = class extends ScopedSymbol {\n  static {\n    __name(this, \"NamespaceSymbol\");\n  }\n  inline;\n  attributes;\n  constructor(name, inline = false, attributes = []) {\n    super(name);\n    this.inline = inline;\n    this.attributes = attributes;\n  }\n};\n\n// src/TypeAlias.ts\nvar TypeAlias = class extends BaseSymbol {\n  static {\n    __name(this, \"TypeAlias\");\n  }\n  targetType;\n  constructor(name, target) {\n    super(name);\n    this.targetType = target;\n  }\n  get baseTypes() {\n    return [this.targetType];\n  }\n  get kind() {\n    return 12 /* Alias */;\n  }\n  get reference() {\n    return 0 /* Irrelevant */;\n  }\n};\n\n// src/CodeCompletionCore.ts\n\n\n// src/utils.ts\nvar longestCommonPrefix = /* @__PURE__ */ __name((arr1, arr2) => {\n  if (!arr1 || !arr2) {\n    return [];\n  }\n  let i;\n  for (i = 0; i < Math.min(arr1.length, arr2.length); i++) {\n    if (arr1[i] !== arr2[i]) {\n      break;\n    }\n  }\n  return arr1.slice(0, i);\n}, \"longestCommonPrefix\");\n\n// src/CodeCompletionCore.ts\nvar CandidatesCollection = class {\n  static {\n    __name(this, \"CandidatesCollection\");\n  }\n  tokens = /* @__PURE__ */ new Map();\n  rules = /* @__PURE__ */ new Map();\n};\nvar FollowSetWithPath = class {\n  static {\n    __name(this, \"FollowSetWithPath\");\n  }\n  intervals;\n  path = [];\n  following = [];\n};\nvar CodeCompletionCore = class _CodeCompletionCore {\n  static {\n    __name(this, \"CodeCompletionCore\");\n  }\n  static followSetsByATN = /* @__PURE__ */ new Map();\n  static atnStateTypeMap = [\n    \"invalid\",\n    \"basic\",\n    \"rule start\",\n    \"block start\",\n    \"plus block start\",\n    \"star block start\",\n    \"token start\",\n    \"rule stop\",\n    \"block end\",\n    \"star loop back\",\n    \"star loop entry\",\n    \"plus loop back\",\n    \"loop end\"\n  ];\n  // Debugging options. Print human readable ATN state and other info.\n  /** Not dependent on showDebugOutput. Prints the collected rules + tokens to terminal. */\n  showResult = false;\n  /** Enables printing ATN state info to terminal. */\n  showDebugOutput = false;\n  /** Only relevant when showDebugOutput is true. Enables transition printing for a state. */\n  debugOutputWithTransitions = false;\n  /** Also depends on showDebugOutput. Enables call stack printing for each rule recursion. */\n  showRuleStack = false;\n  /**\n   * Tailoring of the result:\n   * Tokens which should not appear in the candidates set.\n   */\n  ignoredTokens;\n  /**\n   * Rules which replace any candidate token they contain.\n   * This allows to return descriptive rules (e.g. className, instead of ID/identifier).\n   */\n  preferredRules;\n  /**\n   * Specify if preferred rules should translated top-down (higher index rule returns first) or\n   * bottom-up (lower index rule returns first).\n   */\n  translateRulesTopDown = false;\n  parser;\n  atn;\n  vocabulary;\n  ruleNames;\n  tokens;\n  precedenceStack;\n  tokenStartIndex = 0;\n  statesProcessed = 0;\n  /**\n   * A mapping of rule index + token stream position to end token positions.\n   * A rule which has been visited before with the same input position will always produce the same output positions.\n   */\n  shortcutMap = /* @__PURE__ */ new Map();\n  /** The collected candidates (rules and tokens). */\n  candidates = new CandidatesCollection();\n  constructor(parser) {\n    this.parser = parser;\n    this.atn = parser.atn;\n    this.vocabulary = parser.vocabulary;\n    this.ruleNames = parser.ruleNames;\n    this.ignoredTokens = /* @__PURE__ */ new Set();\n    this.preferredRules = /* @__PURE__ */ new Set();\n  }\n  /**\n   * This is the main entry point. The caret token index specifies the token stream index for the token which\n   * currently covers the caret (or any other position you want to get code completion candidates for).\n   * Optionally you can pass in a parser rule context which limits the ATN walk to only that or called rules.\n   * This can significantly speed up the retrieval process but might miss some candidates (if they are outside of\n   * the given context).\n   *\n   * @param caretTokenIndex The index of the token at the caret position.\n   * @param context An option parser rule context to limit the search space.\n   * @returns The collection of completion candidates.\n   */\n  collectCandidates(caretTokenIndex, context) {\n    this.shortcutMap.clear();\n    this.candidates.rules.clear();\n    this.candidates.tokens.clear();\n    this.statesProcessed = 0;\n    this.precedenceStack = [];\n    this.tokenStartIndex = context?.start ? context.start.tokenIndex : 0;\n    const tokenStream = this.parser.tokenStream;\n    this.tokens = [];\n    let offset = this.tokenStartIndex;\n    while (true) {\n      const token = tokenStream.get(offset++);\n      if (!token) {\n        break;\n      }\n      if (token.channel === antlr4ng__rspack_import_0.Token.DEFAULT_CHANNEL) {\n        this.tokens.push(token);\n        if (token.tokenIndex >= caretTokenIndex || token.type === antlr4ng__rspack_import_0.Token.EOF) {\n          break;\n        }\n      }\n      if (token.type === antlr4ng__rspack_import_0.Token.EOF) {\n        break;\n      }\n    }\n    const callStack = [];\n    const startRule = context ? context.ruleIndex : 0;\n    this.processRule(this.atn.ruleToStartState[startRule], 0, callStack, 0, 0);\n    if (this.showResult) {\n      console.log(`States processed: ${this.statesProcessed}`);\n      console.log(\"\\n\\nCollected rules:\\n\");\n      for (const rule of this.candidates.rules) {\n        let path = \"\";\n        for (const token of rule[1].ruleList) {\n          path += this.ruleNames[token] + \" \";\n        }\n        console.log(this.ruleNames[rule[0]] + \", path: \", path);\n      }\n      const sortedTokens = /* @__PURE__ */ new Set();\n      for (const token of this.candidates.tokens) {\n        let value = this.vocabulary.getDisplayName(token[0]) ?? \"\";\n        for (const following of token[1]) {\n          value += \" \" + this.vocabulary.getDisplayName(following);\n        }\n        sortedTokens.add(value);\n      }\n      console.log(\"\\n\\nCollected tokens:\\n\");\n      for (const symbol of sortedTokens) {\n        console.log(symbol);\n      }\n      console.log(\"\\n\\n\");\n    }\n    return this.candidates;\n  }\n  /**\n   * Checks if the predicate associated with the given transition evaluates to true.\n   *\n   * @param transition The transition to check.\n   * @returns the evaluation result of the predicate.\n   */\n  checkPredicate(transition) {\n    return transition.getPredicate().evaluate(this.parser, antlr4ng__rspack_import_0.ParserRuleContext.EMPTY);\n  }\n  /**\n   * Walks the rule chain upwards or downwards (depending on translateRulesTopDown) to see if that matches any of the\n   * preferred rules. If found, that rule is added to the collection candidates and true is returned.\n   *\n   * @param ruleWithStartTokenList The list to convert.\n   * @returns true if any of the stack entries was converted.\n   */\n  translateStackToRuleIndex(ruleWithStartTokenList) {\n    if (this.preferredRules.size === 0) {\n      return false;\n    }\n    if (this.translateRulesTopDown) {\n      for (let i = ruleWithStartTokenList.length - 1; i >= 0; i--) {\n        if (this.translateToRuleIndex(i, ruleWithStartTokenList)) {\n          return true;\n        }\n      }\n    } else {\n      for (let i = 0; i < ruleWithStartTokenList.length; i++) {\n        if (this.translateToRuleIndex(i, ruleWithStartTokenList)) {\n          return true;\n        }\n      }\n    }\n    return false;\n  }\n  /**\n   * Given the index of a rule from a rule chain, check if that matches any of the preferred rules. If it matches,\n   * that rule is added to the collection candidates and true is returned.\n   *\n   * @param i The rule index.\n   * @param ruleWithStartTokenList The list to check.\n   * @returns true if the specified rule is in the list of preferred rules.\n   */\n  translateToRuleIndex(i, ruleWithStartTokenList) {\n    const { ruleIndex, startTokenIndex } = ruleWithStartTokenList[i];\n    if (this.preferredRules.has(ruleIndex)) {\n      const path = ruleWithStartTokenList.slice(0, i).map(({ ruleIndex: candidate }) => {\n        return candidate;\n      });\n      let addNew = true;\n      for (const rule of this.candidates.rules) {\n        if (rule[0] !== ruleIndex || rule[1].ruleList.length !== path.length) {\n          continue;\n        }\n        if (path.every((v, j) => {\n          return v === rule[1].ruleList[j];\n        })) {\n          addNew = false;\n          break;\n        }\n      }\n      if (addNew) {\n        this.candidates.rules.set(ruleIndex, {\n          startTokenIndex,\n          ruleList: path\n        });\n        if (this.showDebugOutput) {\n          console.log(\"=====> collected: \", this.ruleNames[ruleIndex]);\n        }\n      }\n      return true;\n    }\n    return false;\n  }\n  /**\n   * This method follows the given transition and collects all symbols within the same rule that directly follow it\n   * without intermediate transitions to other rules and only if there is a single symbol for a transition.\n   *\n   * @param transition The transition from which to start.\n   * @returns A list of toke types.\n   */\n  getFollowingTokens(transition) {\n    const result = [];\n    const pipeline = [transition.target];\n    while (pipeline.length > 0) {\n      const state = pipeline.pop();\n      if (state) {\n        state.transitions.forEach((outgoing) => {\n          if (outgoing.serializationType === antlr4ng__rspack_import_0.TransitionType.ATOM) {\n            if (!outgoing.isEpsilon) {\n              const list = outgoing.label.toArray();\n              if (list.length === 1 && !this.ignoredTokens.has(list[0])) {\n                result.push(list[0]);\n                pipeline.push(outgoing.target);\n              }\n            } else {\n              pipeline.push(outgoing.target);\n            }\n          }\n        });\n      }\n    }\n    return result;\n  }\n  /**\n   * Entry point for the recursive follow set collection function.\n   *\n   * @param start Start state.\n   * @param stop Stop state.\n   * @returns Follow sets.\n   */\n  determineFollowSets(start, stop) {\n    const sets = [];\n    const stateStack = [];\n    const ruleStack = [];\n    const isExhaustive = this.collectFollowSets(start, stop, sets, stateStack, ruleStack);\n    const combined = new antlr4ng__rspack_import_0.IntervalSet();\n    for (const set of sets) {\n      combined.addSet(set.intervals);\n    }\n    return { sets, isExhaustive, combined };\n  }\n  /**\n   * Collects possible tokens which could be matched following the given ATN state. This is essentially the same\n   * algorithm as used in the LL1Analyzer class, but here we consider predicates also and use no parser rule context.\n   *\n   * @param s The state to continue from.\n   * @param stopState The state which ends the collection routine.\n   * @param followSets A pass through parameter to add found sets to.\n   * @param stateStack A stack to avoid endless recursions.\n   * @param ruleStack The current rule stack.\n   * @returns true if the follow sets is exhaustive, i.e. we terminated before the rule end was reached, so no\n   * subsequent rules could add tokens\n   */\n  collectFollowSets(s, stopState, followSets, stateStack, ruleStack) {\n    if (stateStack.find((x) => {\n      return x === s;\n    })) {\n      return true;\n    }\n    stateStack.push(s);\n    if (s === stopState || s.stateType === antlr4ng__rspack_import_0.ATNStateType.RULE_STOP) {\n      stateStack.pop();\n      return false;\n    }\n    let isExhaustive = true;\n    for (const transition of s.transitions) {\n      if (transition.serializationType === antlr4ng__rspack_import_0.TransitionType.RULE) {\n        const ruleTransition = transition;\n        if (ruleStack.indexOf(ruleTransition.target.ruleIndex) !== -1) {\n          continue;\n        }\n        ruleStack.push(ruleTransition.target.ruleIndex);\n        const ruleFollowSetsIsExhaustive = this.collectFollowSets(\n          transition.target,\n          stopState,\n          followSets,\n          stateStack,\n          ruleStack\n        );\n        ruleStack.pop();\n        if (!ruleFollowSetsIsExhaustive) {\n          const nextStateFollowSetsIsExhaustive = this.collectFollowSets(\n            ruleTransition.followState,\n            stopState,\n            followSets,\n            stateStack,\n            ruleStack\n          );\n          isExhaustive &&= nextStateFollowSetsIsExhaustive;\n        }\n      } else if (transition.serializationType === antlr4ng__rspack_import_0.TransitionType.PREDICATE) {\n        if (this.checkPredicate(transition)) {\n          const nextStateFollowSetsIsExhaustive = this.collectFollowSets(\n            transition.target,\n            stopState,\n            followSets,\n            stateStack,\n            ruleStack\n          );\n          isExhaustive &&= nextStateFollowSetsIsExhaustive;\n        }\n      } else if (transition.isEpsilon) {\n        const nextStateFollowSetsIsExhaustive = this.collectFollowSets(\n          transition.target,\n          stopState,\n          followSets,\n          stateStack,\n          ruleStack\n        );\n        isExhaustive &&= nextStateFollowSetsIsExhaustive;\n      } else if (transition.serializationType === antlr4ng__rspack_import_0.TransitionType.WILDCARD) {\n        const set = new FollowSetWithPath();\n        set.intervals = antlr4ng__rspack_import_0.IntervalSet.of(antlr4ng__rspack_import_0.Token.MIN_USER_TOKEN_TYPE, this.atn.maxTokenType);\n        set.path = ruleStack.slice();\n        followSets.push(set);\n      } else {\n        let label = transition.label;\n        if (label && label.length > 0) {\n          if (transition.serializationType === antlr4ng__rspack_import_0.TransitionType.NOT_SET) {\n            label = label.complement(antlr4ng__rspack_import_0.Token.MIN_USER_TOKEN_TYPE, this.atn.maxTokenType);\n          }\n          const set = new FollowSetWithPath();\n          set.intervals = label ?? new antlr4ng__rspack_import_0.IntervalSet();\n          set.path = ruleStack.slice();\n          set.following = this.getFollowingTokens(transition);\n          followSets.push(set);\n        }\n      }\n    }\n    stateStack.pop();\n    return isExhaustive;\n  }\n  /**\n   * Walks the ATN for a single rule only. It returns the token stream position for each path that could be matched\n   * in this rule.\n   * The result can be empty in case we hit only non-epsilon transitions that didn't match the current input or if we\n   * hit the caret position.\n   *\n   * @param startState The start state.\n   * @param tokenListIndex The token index we are currently at.\n   * @param callStack The stack that indicates where in the ATN we are currently.\n   * @param precedence The current precedence level.\n   * @param indentation A value to determine the current indentation when doing debug prints.\n   * @returns the set of token stream indexes (which depend on the ways that had to be taken).\n   */\n  processRule(startState, tokenListIndex, callStack, precedence, indentation) {\n    let positionMap = this.shortcutMap.get(startState.ruleIndex);\n    if (!positionMap) {\n      positionMap = /* @__PURE__ */ new Map();\n      this.shortcutMap.set(startState.ruleIndex, positionMap);\n    } else {\n      if (positionMap.has(tokenListIndex)) {\n        if (this.showDebugOutput) {\n          console.log(\"=====> shortcut\");\n        }\n        return positionMap.get(tokenListIndex);\n      }\n    }\n    const result = /* @__PURE__ */ new Set();\n    let setsPerState = _CodeCompletionCore.followSetsByATN.get(this.parser.constructor.name);\n    if (!setsPerState) {\n      setsPerState = /* @__PURE__ */ new Map();\n      _CodeCompletionCore.followSetsByATN.set(this.parser.constructor.name, setsPerState);\n    }\n    let followSets = setsPerState.get(startState.stateNumber);\n    if (!followSets) {\n      const stop = this.atn.ruleToStopState[startState.ruleIndex];\n      followSets = this.determineFollowSets(startState, stop);\n      setsPerState.set(startState.stateNumber, followSets);\n    }\n    const startTokenIndex = this.tokens[tokenListIndex].tokenIndex;\n    callStack.push({\n      startTokenIndex,\n      ruleIndex: startState.ruleIndex\n    });\n    if (tokenListIndex >= this.tokens.length - 1) {\n      if (this.preferredRules.has(startState.ruleIndex)) {\n        this.translateStackToRuleIndex(callStack);\n      } else {\n        for (const set of followSets.sets) {\n          const fullPath = callStack.slice();\n          const followSetPath = set.path.map((path) => {\n            return {\n              startTokenIndex,\n              ruleIndex: path\n            };\n          });\n          fullPath.push(...followSetPath);\n          if (!this.translateStackToRuleIndex(fullPath)) {\n            for (const symbol of set.intervals.toArray()) {\n              if (!this.ignoredTokens.has(symbol)) {\n                if (this.showDebugOutput) {\n                  console.log(\"=====> collected: \", this.vocabulary.getDisplayName(symbol));\n                }\n                if (!this.candidates.tokens.has(symbol)) {\n                  this.candidates.tokens.set(symbol, set.following);\n                } else {\n                  if (this.candidates.tokens.get(symbol) !== set.following) {\n                    this.candidates.tokens.set(symbol, []);\n                  }\n                }\n              }\n            }\n          }\n        }\n      }\n      if (!followSets.isExhaustive) {\n        result.add(tokenListIndex);\n      }\n      callStack.pop();\n      return result;\n    } else {\n      const currentSymbol = this.tokens[tokenListIndex].type;\n      if (followSets.isExhaustive && !followSets.combined.contains(currentSymbol)) {\n        callStack.pop();\n        return result;\n      }\n    }\n    if (startState.isPrecedenceRule) {\n      this.precedenceStack.push(precedence);\n    }\n    const statePipeline = [];\n    let currentEntry;\n    statePipeline.push({ state: startState, tokenListIndex });\n    while (statePipeline.length > 0) {\n      currentEntry = statePipeline.pop();\n      ++this.statesProcessed;\n      const currentSymbol = this.tokens[currentEntry.tokenListIndex].type;\n      const atCaret = currentEntry.tokenListIndex >= this.tokens.length - 1;\n      if (this.showDebugOutput) {\n        this.printDescription(\n          indentation,\n          currentEntry.state,\n          this.generateBaseDescription(currentEntry.state),\n          currentEntry.tokenListIndex\n        );\n        if (this.showRuleStack) {\n          this.printRuleState(callStack);\n        }\n      }\n      if (currentEntry.state.stateType === antlr4ng__rspack_import_0.ATNStateType.RULE_STOP) {\n        result.add(currentEntry.tokenListIndex);\n        continue;\n      }\n      const transitions = currentEntry.state.transitions;\n      for (const transition of transitions) {\n        switch (transition.serializationType) {\n          case antlr4ng__rspack_import_0.TransitionType.RULE: {\n            const ruleTransition = transition;\n            const endStatus = this.processRule(\n              transition.target,\n              currentEntry.tokenListIndex,\n              callStack,\n              ruleTransition.precedence,\n              indentation + 1\n            );\n            for (const position of endStatus) {\n              statePipeline.push({\n                state: transition.followState,\n                tokenListIndex: position\n              });\n            }\n            break;\n          }\n          case antlr4ng__rspack_import_0.TransitionType.PREDICATE: {\n            if (this.checkPredicate(transition)) {\n              statePipeline.push({\n                state: transition.target,\n                tokenListIndex: currentEntry.tokenListIndex\n              });\n            }\n            break;\n          }\n          case antlr4ng__rspack_import_0.TransitionType.PRECEDENCE: {\n            const predTransition = transition;\n            if (predTransition.precedence >= this.precedenceStack[this.precedenceStack.length - 1]) {\n              statePipeline.push({\n                state: transition.target,\n                tokenListIndex: currentEntry.tokenListIndex\n              });\n            }\n            break;\n          }\n          case antlr4ng__rspack_import_0.TransitionType.WILDCARD: {\n            if (atCaret) {\n              if (!this.translateStackToRuleIndex(callStack)) {\n                for (const token of antlr4ng__rspack_import_0.IntervalSet.of(antlr4ng__rspack_import_0.Token.MIN_USER_TOKEN_TYPE, this.atn.maxTokenType).toArray()) {\n                  if (!this.ignoredTokens.has(token)) {\n                    this.candidates.tokens.set(token, []);\n                  }\n                }\n              }\n            } else {\n              statePipeline.push({\n                state: transition.target,\n                tokenListIndex: currentEntry.tokenListIndex + 1\n              });\n            }\n            break;\n          }\n          default: {\n            if (transition.isEpsilon) {\n              statePipeline.push({\n                state: transition.target,\n                tokenListIndex: currentEntry.tokenListIndex\n              });\n              continue;\n            }\n            let set = transition.label;\n            if (set && set.length > 0) {\n              if (transition.serializationType === antlr4ng__rspack_import_0.TransitionType.NOT_SET) {\n                set = set.complement(antlr4ng__rspack_import_0.Token.MIN_USER_TOKEN_TYPE, this.atn.maxTokenType);\n              }\n              if (atCaret) {\n                if (!this.translateStackToRuleIndex(callStack)) {\n                  const list = set.toArray();\n                  const hasTokenSequence = list.length === 1;\n                  for (const symbol of list) {\n                    if (!this.ignoredTokens.has(symbol)) {\n                      if (this.showDebugOutput) {\n                        console.log(\n                          \"=====> collected: \",\n                          this.vocabulary.getDisplayName(symbol)\n                        );\n                      }\n                      const followingTokens = hasTokenSequence ? this.getFollowingTokens(transition) : [];\n                      if (!this.candidates.tokens.has(symbol)) {\n                        this.candidates.tokens.set(symbol, followingTokens);\n                      } else {\n                        this.candidates.tokens.set(\n                          symbol,\n                          longestCommonPrefix(\n                            followingTokens,\n                            this.candidates.tokens.get(symbol)\n                          )\n                        );\n                      }\n                    }\n                  }\n                }\n              } else {\n                if (set.contains(currentSymbol)) {\n                  if (this.showDebugOutput) {\n                    console.log(\"=====> consumed: \", this.vocabulary.getDisplayName(currentSymbol));\n                  }\n                  statePipeline.push({\n                    state: transition.target,\n                    tokenListIndex: currentEntry.tokenListIndex + 1\n                  });\n                }\n              }\n            }\n          }\n        }\n      }\n    }\n    callStack.pop();\n    if (startState.isPrecedenceRule) {\n      this.precedenceStack.pop();\n    }\n    positionMap.set(tokenListIndex, result);\n    return result;\n  }\n  generateBaseDescription(state) {\n    const stateValue = state.stateNumber === antlr4ng__rspack_import_0.ATNState.INVALID_STATE_NUMBER ? \"Invalid\" : state.stateNumber;\n    return `[${stateValue} ${_CodeCompletionCore.atnStateTypeMap[state.stateType]}] in ${this.ruleNames[state.ruleIndex]}`;\n  }\n  printDescription(indentation, state, baseDescription, tokenIndex) {\n    const indent = \"  \".repeat(indentation);\n    let output = indent;\n    let transitionDescription = \"\";\n    if (this.debugOutputWithTransitions) {\n      for (const transition of state.transitions) {\n        let labels = \"\";\n        const symbols = transition.label ? transition.label.toArray() : [];\n        if (symbols.length > 2) {\n          labels = this.vocabulary.getDisplayName(symbols[0]) + \" .. \" + this.vocabulary.getDisplayName(symbols[symbols.length - 1]);\n        } else {\n          for (const symbol of symbols) {\n            if (labels.length > 0) {\n              labels += \", \";\n            }\n            labels += this.vocabulary.getDisplayName(symbol);\n          }\n        }\n        if (labels.length === 0) {\n          labels = \"\\u03B5\";\n        }\n        transitionDescription += `\n${indent}\t(${labels}) [${transition.target.stateNumber} ${_CodeCompletionCore.atnStateTypeMap[transition.target.stateType]}] in ${this.ruleNames[transition.target.ruleIndex]}`;\n      }\n    }\n    if (tokenIndex >= this.tokens.length - 1) {\n      output += `<<${this.tokenStartIndex + tokenIndex}>> `;\n    } else {\n      output += `<${this.tokenStartIndex + tokenIndex}> `;\n    }\n    console.log(output + \"Current state: \" + baseDescription + transitionDescription);\n  }\n  printRuleState(stack) {\n    if (stack.length === 0) {\n      console.log(\"<empty stack>\");\n      return;\n    }\n    for (const rule of stack) {\n      console.log(this.ruleNames[rule.ruleIndex]);\n    }\n  }\n};\n\n// src/SymbolTable.ts\nvar SymbolTable = class extends ScopedSymbol {\n  constructor(name, options) {\n    super(name);\n    this.options = options;\n  }\n  static {\n    __name(this, \"SymbolTable\");\n  }\n  /**  Other symbol information available to this instance. */\n  dependencies = /* @__PURE__ */ new Set();\n  get info() {\n    return {\n      dependencyCount: this.dependencies.size,\n      symbolCount: this.children.length\n    };\n  }\n  clear() {\n    super.clear();\n    this.dependencies.clear();\n  }\n  addDependencies(...tables) {\n    tables.forEach((value) => {\n      this.dependencies.add(value);\n    });\n  }\n  removeDependency(table) {\n    if (this.dependencies.has(table)) {\n      this.dependencies.delete(table);\n    }\n  }\n  addNewSymbolOfType(t, parent, ...args) {\n    const result = new t(...args);\n    if (!parent || parent === this) {\n      this.addSymbol(result);\n    } else {\n      parent.addSymbol(result);\n    }\n    return result;\n  }\n  async addNewNamespaceFromPath(parent, path, delimiter = \".\") {\n    const parts = path.split(delimiter);\n    let i = 0;\n    let currentParent = parent === void 0 ? this : parent;\n    while (i < parts.length - 1) {\n      let namespace = await currentParent.resolve(parts[i], true);\n      if (namespace === void 0) {\n        namespace = this.addNewSymbolOfType(NamespaceSymbol, currentParent, parts[i]);\n      }\n      currentParent = namespace;\n      ++i;\n    }\n    return this.addNewSymbolOfType(NamespaceSymbol, currentParent, parts[parts.length - 1]);\n  }\n  addNewNamespaceFromPathSync(parent, path, delimiter = \".\") {\n    const parts = path.split(delimiter);\n    let i = 0;\n    let currentParent = parent === void 0 ? this : parent;\n    while (i < parts.length - 1) {\n      let namespace = currentParent.resolveSync(parts[i], true);\n      if (namespace === void 0) {\n        namespace = this.addNewSymbolOfType(NamespaceSymbol, currentParent, parts[i]);\n      }\n      currentParent = namespace;\n      ++i;\n    }\n    return this.addNewSymbolOfType(NamespaceSymbol, currentParent, parts[parts.length - 1]);\n  }\n  async getAllSymbols(t, localOnly = false) {\n    const result = await super.getAllSymbols(t, localOnly);\n    if (!localOnly) {\n      const dependencyResults = await Promise.all([...this.dependencies].map((dependency) => {\n        return dependency.getAllSymbols(t, localOnly);\n      }));\n      dependencyResults.forEach((value) => {\n        result.push(...value);\n      });\n    }\n    return result;\n  }\n  getAllSymbolsSync(t, localOnly = false) {\n    const result = super.getAllSymbolsSync(t, localOnly);\n    if (!localOnly) {\n      this.dependencies.forEach((dependency) => {\n        result.push(...dependency.getAllSymbolsSync(t, localOnly));\n      });\n    }\n    return result;\n  }\n  async symbolWithContext(context) {\n    const findRecursive = /* @__PURE__ */ __name((symbol) => {\n      if (symbol.context === context) {\n        return symbol;\n      }\n      if (symbol instanceof ScopedSymbol) {\n        for (const child of symbol.children) {\n          const result = findRecursive(child);\n          if (result) {\n            return result;\n          }\n        }\n      }\n      return void 0;\n    }, \"findRecursive\");\n    let symbols = await this.getAllSymbols(BaseSymbol);\n    for (const symbol of symbols) {\n      const result = findRecursive(symbol);\n      if (result) {\n        return result;\n      }\n    }\n    for (const dependency of this.dependencies) {\n      symbols = await dependency.getAllSymbols(BaseSymbol);\n      for (const symbol of symbols) {\n        const result = findRecursive(symbol);\n        if (result) {\n          return result;\n        }\n      }\n    }\n    return void 0;\n  }\n  symbolWithContextSync(context) {\n    const findRecursive = /* @__PURE__ */ __name((symbol) => {\n      if (symbol.context === context) {\n        return symbol;\n      }\n      if (symbol instanceof ScopedSymbol) {\n        for (const child of symbol.children) {\n          const result = findRecursive(child);\n          if (result) {\n            return result;\n          }\n        }\n      }\n      return void 0;\n    }, \"findRecursive\");\n    let symbols = this.getAllSymbolsSync(BaseSymbol);\n    for (const symbol of symbols) {\n      const result = findRecursive(symbol);\n      if (result) {\n        return result;\n      }\n    }\n    for (const dependency of this.dependencies) {\n      symbols = dependency.getAllSymbolsSync(BaseSymbol);\n      for (const symbol of symbols) {\n        const result = findRecursive(symbol);\n        if (result) {\n          return result;\n        }\n      }\n    }\n    return void 0;\n  }\n  async resolve(name, localOnly = false) {\n    let result = await super.resolve(name, localOnly);\n    if (!result && !localOnly) {\n      for (const dependency of this.dependencies) {\n        result = await dependency.resolve(name, false);\n        if (result) {\n          return result;\n        }\n      }\n    }\n    return result;\n  }\n  resolveSync(name, localOnly = false) {\n    let result = super.resolveSync(name, localOnly);\n    if (!result && !localOnly) {\n      for (const dependency of this.dependencies) {\n        result = dependency.resolveSync(name, false);\n        if (result) {\n          return result;\n        }\n      }\n    }\n    return result;\n  }\n};\n\n\n\n//# sourceURL=javascript/esm%7C/Users/qinyoubing/Desktop/dataops/code-editor/node_modules/.pnpm/antlr4-c3@3.3.7_antlr4ng-cli@1.0.7/node_modules/antlr4-c3/lib/index.mjs\n}");

},

}]);